# Dofixo — سند انتقال پروژه

> این سند برای ادامه‌ی کار مهاجرت Dofixo از تک‌مستأجری به SaaS در یک نشست جدید نوشته
> شده است. همه‌چیزی که برای ادامه لازم است اینجاست: وضعیت فعلی، تصمیمات گرفته‌شده و
> دلیلشان، تسک‌های باقی‌مانده، بدهی‌های فنی، و دام‌هایی که در مسیر به آن‌ها خوردیم.

> **تاریخ آخرین به‌روزرسانی:** فاز ۸ **مستقر شد** (۲ سپتامبر ۲۰۲۶) —
> اشتراک، پرداخت زیبال، رفرال، cron شبانه و رابط کاربری روی پروداکشن‌اند.
> یک شکاف باقی است: پیامک تأیید پرداخت (تسک ۸.۱۰).

---

## ۱. پروژه در یک نگاه

**Dofixo** یک نرم‌افزار مدیریت تعمیرگاه است برای کارگاه‌های کوچک تعمیر (لوازم خانگی،
موبایل، لپ‌تاپ) در ایران.

| مورد       | مقدار                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| مخزن       | `/Programming/Code/Dofixo` (مونوریپو: `backend/`، `frontend/`، `ops/`) |
| برنچ فعال  | `feature/multi-tenant-migration`                                       |
| دامنه‌ی اپ | `app.dofixo.ir` — **دامنه‌ی مشترک، بدون زیردامنه برای هر مستأجر**      |
| سایت معرفی | `dofixo.ir` (Astro، روی Vercel، مخزن جدا)                              |
| زبان رابط  | فارسی، تاریخ جلالی                                                     |
| واحد پول   | ریال                                                                   |

**هدف اصلی:** تبدیل زیرساخت به SaaS **بدون تغییر مجموعه‌ی ویژگی‌های موجود**.

---

## ۲. وضعیت فعلی

| فاز                            | وضعیت                                   |
| ------------------------------ | --------------------------------------- |
| ۰ — زیرساخت و ابزار            | ✅ کامل                                 |
| ۱ — مهاجرت SQLite → PostgreSQL | ✅ کامل                                 |
| ۲ — چندمستأجری                 | ✅ کامل                                 |
| ۳ — بازنویسی احراز هویت        | ✅ کامل                                 |
| ۴ — Object Storage             | ✅ کامل                                 |
| ۵ — بکاپ و خروجی داده          | 🔄 همه تمام جز ۵.۶ (به تعویق افتاده)    |
| OTP — تأیید شماره با پیامک     | ✅ کامل (OTP.1 تا OTP.6)                |
| ۶.۴ — تست یکپارچگی             | ✅ (همراه ۲.۷)                          |
| TypeScript فرانت‌اند           | ✅ کامل                                 |
| **۷ — استقرار**                | **✅ مستقر شد** (فقط ۷.۷ باقی)          |
| **۸ — اشتراک و پرداخت**        | **✅ مستقر شد** (۸.۱ تا ۸.۹ · ۸.۱۰ باز) |

**وضعیت اپ:** روی `https://app.dofixo.ir` زنده است. ثبت‌نام با پیامک واقعی،
session با کوکی refresh، ایزولاسیون اثبات‌شده، عکس‌ها روی object storage
پروداکشن، بکاپ رمزنگاری‌شده‌ی شبانه که بازیابی‌اش تست شده، و از ۲ سپتامبر
اشتراک و پرداخت زیبال.

✅ **پروداکشن دیگر پشت برنچ نیست.** چهار مهاجرت فاز ۸ اجرا شدند، شش متغیر
تازه در `.env.prod` نشستند، و cron اشتراک روی `0 4` نصب شد. یک پرداخت واقعی
تأیید شد: `refNumber` و `cardNumber` هر دو پر برگشتند.

🔴 **تنها شکاف: پیامک تأیید پرداخت نمی‌رود** — تسک ۸.۱۰. مشتری پرداخت
می‌کند، اشتراکش تمدید می‌شود، رسیدش شماره پیگیری دارد، و هیچ پیامی دریافت
نمی‌کند.

⚠️ **انقضای ۲۹ سپتامبر رفع شد.** backfill مهاجرت ۸.۱ روی هر سه کارگاه
موجود `never_expires = TRUE` گذاشت.

🔴 **پروداکشن پشت برنچ است.** **چهار** مهاجرت فاز ۸ روی سرور اجرا نشده‌اند و
`.env.prod` **هفت متغیر تازه** ندارد. تا وقتی مستقر نشود، ستون
`never_expires` وجود ندارد و کارگاه موجود روی **۲۹ سپتامبر ۲۰۲۶** منقضی
می‌شود. چک‌لیست کامل در §۱۲.

### فاز ۵ — چه چیزی ساخته شد

| تسک                        | خلاصه                                                        |
| -------------------------- | ------------------------------------------------------------ |
| 5.1 بررسی بکاپ ارائه‌دهنده | جواب گرفت — تصمیم: تک‌سرور با بکاپ خودمان (دلیلش پایین)      |
| 5.2 بکاپ زمان‌بندی‌شده     | `ops/backup-database.sh` — رمزنگاری با `age`، آپلود به Arvan |
| 5.3 خروجی داده‌ی هر کارگاه | `/api/exports` — Excel هفت‌برگه‌ای + zip عکس‌ها، در پس‌زمینه |
| 5.4 صفحه‌ی خروجی           | `ExportList.tsx` در `/exports` — جایگزین صفحه و API بکاپ     |
| 5.5 بازیابی یک کارگاه      | `ops/extract-workspace.sh` + `ops/restore-workspace.md`      |
| 5.7 بازیابی دسترسی کاربر   | `ops/reset-password.sh` + `ops/reset-password.md`            |

---

### مهاجرت TypeScript فرانت — چه چیزی عوض شد

بیرون از شماره‌گذاری فازها، قبل از ۵.۴ انجام شد تا صفحه‌ی خروجی داده از خط
اول TypeScript باشد. تدریجی با `allowJs`، و `strict: true` از همان ابتدا.

| بخش             | فایل | نکته                                                   |
| --------------- | ---- | ------------------------------------------------------ |
| زیرساخت         | ۴    | tsconfig · eslint · vite-env · اعلان `jalaali-js`      |
| `utils/` `api/` | ۳    | `types/api.ts` شکل هر endpoint را از کنترلرش می‌گوید   |
| `context/`      | ۳    | ModalContext آخر، بعد از ۱۳ مودالی که رندر می‌کند      |
| `components/`   | ۲۷   | چهار دسته: برگ‌ها، عکس و تاریخ، مودال‌های CRUD، بقیه   |
| `pages/`        | ۱۴   | —                                                      |
| ریشه            | ۴    | App · main · vite.config · HomeIcon از `public/` بیرون |

**`allowJs` بسته شد.** یک فایل `.js` جدید در `src/` حالا خطای کامپایل است.

**`tsc --noEmit` داخل `pnpm build` است.** فرانت تست خودکار ندارد، پس کامپایلر
تنها دروازه‌ای است که دارد. اگر بیرون از build بماند، در عمل زده نمی‌شود.

### خروجی داده — تصمیم‌ها

**در پس‌زمینه، نه در درخواست.** کارگاهی با هزار دستگاه دقایق طول می‌کشد، که
از عمر هر اتصالی که یک reverse proxy باز نگه می‌دارد بیشتر است. `setImmediate`
نه صف: نه Redis هست نه worker، و این استقرار یک process دارد.

**ردیفی که `pending` مانده یعنی process مرده.** بعد از سی دقیقه دیگر مانع
درخواست تازه نمی‌شود و قابل حذف است — بدون آن، کارگاه در بن‌بستی می‌ماند که
نه می‌تواند تمامش کند نه پاکش.

**کوئری‌ها سریال‌اند، نه `Promise.all`.** شش کوئری سنگین همزمان بیشتر استخر
ده‌اتصالی را می‌گرفت، آن‌قدر که یک درخواست عادی کنارش اتصال گیر نمی‌آورد.
کسی منتظر build نیست، پس چند ثانیه کندتر بودن هزینه‌ای ندارد.

**`buildExport` خودش context را باز می‌کند** با `runWithWorkspace` — همان شکل
مسئله‌ای که multer می‌سازد، از جهت مخالف: تا وقتی این اجرا شود، store درخواست
رفته است.

**تاریخ‌ها جلالی، مبالغ عدد.** صاحب تعمیرگاه `2026-01-15T10:30:00Z` را
نمی‌خواند، و اگر مبلغ را رشته‌ی فرمت‌شده بنویسیم، Excel جمعش نمی‌زند.

⚠️ **`archiver` روی ۷ می‌ماند.** نسخه‌ی ۸ فقط ESM است و **هر شش سوئیت
یکپارچگی** را می‌خواباند (نه فقط تست خروجی — چون `app.ts` همه‌ی روت‌ها را
وارد می‌کند)، و پروداکشن را به `require(esm)` نود وابسته می‌کرد.

### تأیید شماره با پیامک — چه چیزی ساخته شد

| تسک   | خلاصه                                                          |
| ----- | -------------------------------------------------------------- |
| OTP.1 | جدول `otp_codes` — تنها جدول بدون `workspace_id`               |
| OTP.2 | `lib/sms.ts` — تنها ماژولی که sms.ir را می‌شناسد               |
| OTP.3 | `POST /auth/send-otp` با دو سقف مستقل                          |
| OTP.4 | ثبت‌نام کد تأییدشده می‌خواهد                                   |
| OTP.5 | `POST /auth/reset-password`                                    |
| OTP.6 | `OtpCodeStep` مشترک · `Register` دومرحله‌ای · `ForgotPassword` |

**بدون driver interface.** روادمپ یکی می‌خواست با driver `console` برای
توسعه. ساخته نشد: تست‌ها ماژول را mock می‌کنند، همان‌طور که برای
`lib/storage` می‌کنند، و پیاده‌سازی دوم مسیری است که هیچ‌وقت در پروداکشن
اجرا نمی‌شود.

**`OtpCode` از guard مستثناست** (`UNSCOPED_MODELS` در `lib/prisma.ts`).
`send-otp` قبل از هر workspace ای اجرا می‌شود، پس guard که عمداً پرتاب
می‌کند این جدول را غیرقابل استفاده می‌کرد.

⚠️ **این استثنا فقط چون policy اش `USING (true)` است امن است.** نام دیگری
در آن مجموعه یعنی جدولی که بی‌صدا صفر ردیف برمی‌گرداند — همان خاموشی که
کل extension برای جلوگیری از آن نوشته شد.

**شمارنده‌ی تلاش بیرون از تراکنش، مصرف کد داخلش.** دو نوشتن در دو جهت
مخالف، و هر دو لازم. اگر `attempts` داخل تراکنش زیاد می‌شد، rollback برش
می‌گرداند و سقف سه تلاش روی کدی صد‌هزارحالته هرگز نمی‌رسید. اگر
`consumedAt` بیرون بود، ثبت‌نامی که روی شماره‌ی تکراری شکست می‌خورد یکی از
سه سهمیه‌ی کاربر را برای خطای کس دیگری می‌سوزاند.

**ارسال ناموفق هیچ سقفی را مصرف نمی‌کند.** ردیف حذف می‌شود و limiter
`skipFailedRequests` دارد. قطعی چنددقیقه‌ای sms.ir نباید کاربر را یک ساعت
قفل کند برای پیامکی که نرفت.

**بازیابی رمز session تازه نمی‌دهد.** هدفش پایان دادن به همه‌ی session ها
بود؛ باز کردن یکی در همان نفس نیمی از آن را خنثی می‌کند.

### زیرساخت پروداکشن (۷.۵ ✅)

| مورد        | مقدار                                                     |
| ----------- | --------------------------------------------------------- |
| ارائه‌دهنده | پارس‌وی‌دی‌اس، IR_VPS_05                                  |
| IP          | `45.156.186.60` — **اختصاصی، مستقیم روی eth0** (NAT نیست) |
| پورت SSH    | **۹۰۱۱**، نه ۲۲                                           |
| مشخصات      | ۴ هسته · ۹٫۷ گیگ رم · ۷۹ گیگ · Ubuntu 24.04               |
| هاست‌نیم    | `fastcreate-379254`                                       |
| دسترسی      | `ssh dofixo` (کاربر `reza`، کلید ed25519)                 |

**سخت‌سازی انجام‌شده:** رمز root عوض شد · کاربر `reza` با sudo · ورود فقط با
کلید (`PasswordAuthentication no` + `PermitRootLogin no` در
`sshd_config.d/99-hardening.conf`، نه فایل اصلی، تا آپدیت اوبونتو رویش
ننویسد) · کلید غریبه پاک شد · سیستم به‌روز (کرنل 6.8.0-138) · fail2ban (از
قبل در ایمیج بود) · ufw با ۹۰۱۱/۸۰/۴۴۳ · swap دو گیگی با `vm.swappiness=10`

**اینترنت بین‌الملل:** فرم رگولاتوری پر شد ولی **هرگز فعال نشد**. اپ در حال
اجرا **صفر** وابستگی بین‌الملل دارد — sms.ir، آروان و زیبال همه داخلی. دو
چیزی که به آن نیاز داشتند دور زده شدند: گواهی TLS دستی گرفته شد (۷.۴) و
ایمیج‌ها با `docker save` منتقل می‌شوند (۷.۶).

**IP در پنل sms.ir ثبت شد ✅** (`45.156.186.60`) — اگر روزی سرور عوض شود
باید دوباره ثبت شود.

### ۷.۶ — استقرار (انجام شد)

**ایمیج‌ها روی لپ‌تاپ ساخته و با `docker save` منتقل می‌شوند.** سرور به Docker
Hub و npm نمی‌رسد، پس هرگز روی سرور `build` زده نمی‌شود.

⚠️ **هر سه سرویس تگ `:prod` صریح دارند.** بدون آن، compose پروداکشن **ایمیج
توسعه را بازنویسی می‌کند** — و آنچه روی سرور می‌رود `tsx watch` با root و
بدون `NODE_ENV` است، که تا مدت‌ها معلوم نمی‌شود. `migrate` هم تگ می‌خواهد،
وگرنه روی سرور دنبال `./backend` می‌گردد که وجود ندارد.

**مخزن روی سرور نیست.** فقط `~/dofixo/` با پنج فایل: `docker-compose.prod.yml`
· `Caddyfile` · `.env.prod` · `backup-database.sh` · `backup.env`.
به‌علاوه‌ی `~/certs/` برای گواهی.

**تأییدهای واقعی روی پروداکشن:**

- ثبت‌نام با پیامک واقعی
- مشتری، دستگاه، و آپلود عکس با rotate و thumbnail روی `dofixo-prod`
- خروجی داده و دانلودش
- بکاپ و **بازیابی تست‌شده**: dump روی لپ‌تاپ رمزگشایی شد و ۲۱ policy، هر
  چهار تابع `app_*` و سه ردیف `roles` با متن فارسی سالم در آن بودند
- cron با پنج اجرای موفق پشت سر هم
- پورت ۵۴۳۲ و ۵۰۰۱ از اینترنت **timeout** — فقط ۸۰ و ۴۴۳ جواب می‌دهند

### فاز ۸ — چه چیزی ساخته شد (۸.۱ تا ۸.۵)

| تسک | خلاصه                                                         |
| --- | ------------------------------------------------------------- |
| 8.1 | هشت مدل + ۷ policy + سه پلن + backfill                        |
| 8.2 | `utils/subscription.ts` — تنها جایی که `expiresAt` عوض می‌شود |
| 8.3 | نگهبان فقط-خواندنی با ۴۰۲، از داخل `authenticate`             |
| 8.4 | `utils/pricing.ts` — تابع خالص، تخفیف بزرگ‌تر برنده           |
| 8.5 | `lib/zibal.ts` + کنترلر و روت اشتراک + تسویه‌ی پرداخت         |
| 8.6 | رفرال — کد در ثبت‌نام، پاداش پس از تأیید پرداخت دعوت‌شده      |
| 8.7 | cron روی هاست — یادآوری، پیامک، فقط-خواندنی، حذف، تسویه       |
| 8.8 | صفحه‌ی اشتراک، پیش‌نمایش قیمت، بنر، صفحه‌ی بازگشت             |
| 8.9 | صفحه‌ی دعوت، فیلد کد در ثبت‌نام، رسید قابل چاپ                |

**قواعد محصولی که قفل شدند:**

| مورد           | مقدار                                            |
| -------------- | ------------------------------------------------ |
| آزمایشی        | ۳۰ روز، بدون محدودیت ویژگی، بدون کارت بانکی      |
| پلن‌ها         | ۹۰ روز / ۱۸۰ روز / **۴۲۵ روز** (۱۲+۲ ماه)        |
| قیمت‌ها (ریال) | ۱۹٬۹۰۰٬۰۰۰ · ۳۹٬۹۰۰٬۰۰۰ · ۷۹٬۹۰۰٬۰۰۰             |
| مهلت ارفاق     | ۳ روز پس از انقضا، اپ کامل کار می‌کند            |
| فقط-خواندنی    | روز ۳ تا ۳۰                                      |
| حذف داده       | روز ۳۰ — **همان عددی که به مشتری گفته می‌شود**   |
| رفرال          | دعوت‌شده ۱۰٪ تخفیف · دعوت‌کننده ۳۰ روز، بدون سقف |
| تخفیف‌ها       | جمع نمی‌شوند، بزرگ‌تر برنده                      |
| گرد کردن       | ۱۰٬۰۰۰ ریال، رو به پایین                         |
| پرداخت‌کننده   | ادمین و سوپرادمین · پیامک فقط به سوپرادمین       |

⚠️ **عدد حذف داده دروغ نیست، عمداً.** گزینه‌ی «بگو یک هفته، سی روز نگه دار»
بررسی و رد شد: مشتری‌ای که دیرتر برمی‌گردد و داده‌اش را سر جایش می‌بیند، به
بقیه‌ی حرف‌های ما درباره‌ی بکاپ هم شک می‌کند. فشار از تعداد یادآوری‌ها می‌آید
نه از عدد.

**پنج قالب پیامک تأیید شده در sms.ir:**

| قالب                | شناسه    | کِی                       |
| ------------------- | -------- | ------------------------- |
| پیش از پایان اشتراک | `398956` | ۷ روز مانده · ۱ روز مانده |
| روز پایان اشتراک    | `764207` | روز ۰                     |
| پس از پایان اشتراک  | `450597` | روز ۳ · روز ۲۳            |
| پرداخت موفق         | `344895` | پس از تأیید               |
| پاداش دعوت          | `549585` | پس از پرداخت دعوت‌شده     |

⚠️ **تاریخ در پیامک `۱۴۰۵-۰۹-۱۲` است، نه با `/`.** نه چون ممنوع است — چون
نمی‌دانیم ممنوع هست یا نه، و علامتش `HTTP 400` می‌بود که در لاگ عیناً شبیه
«شناسه‌ی قالب اشتباه» است. سقف پارامتر ۴۰ کاراکتر.

⚠️ **قالب «پرداخت موفق» در دسته‌ی «وضعیت سفارش» ثبت شده، بقیه «اطلاع‌رسانی».**
اگر روزی چهارتا رفتند و این یکی نه، اول اینجا را نگاه کن نه کد را.

### درگاه زیبال — آنچه پشتیبانی گفت

- **مرچنت `dofixo.ir` برای `app.dofixo.ir` کار می‌کند.** درگاه دوم لازم نیست.
- **IP سرور باید در پنل ثبت شود** — `45.156.186.60` ثبت شد. اگر نباشد،
  کد نتیجه‌ی `115` می‌آید که برخلاف sms.ir صریح است.
- **تست با مرچنت `ZIBAL`** — همه‌ی قابلیت‌ها بدون جابه‌جایی پول.
- **کارمزد همیشه از پذیرنده**، به دستور شاپرک. یعنی مبلغی که کاربر می‌بیند
  دقیقاً قیمت پلن است — یک عدد، نه دو تا.

---

## ۳. پشته‌ی فنی

### Backend

```
Node 26 · TypeScript 5.9 · Express 5 · pnpm 10.10.0

Prisma 7.9.1 + @prisma/adapter-pg + pg 8.22 · PostgreSQL 17 (Docker)
Zod 4.4.3 · helmet 8.3 · express-rate-limit 8.6 · cookie-parser 1.4
@aws-sdk/client-s3 + @aws-sdk/s3-request-presigner
bcryptjs · jsonwebtoken · multer · sharp · jalaali-js
Jest 30 + ts-jest 29.4 + supertest
```

### Frontend

```
React 19 · Vite 8 · React Router 7 · Tailwind 4 · TypeScript 5.9
Axios · react-hot-toast · react-to-print · jalaali-js · @heroicons/react
```

⚠️ **فرانت هیچ زیرساخت تستی ندارد.** TypeScript تنها بررسی خودکارش است.
Vitest هنوز تسک باز فاز ۶ است.

⚠️ **TypeScript روی ۵.۹ پین است، نه ۷.** `typescript-eslint@8` با TS 7 بارگذاری
نمی‌شود (issue #10940) و کل اجرای lint را می‌خواباند، نه فقط بلوک TS را.

### ابزارهای عملیاتی روی هاست

```
age (رمزنگاری بکاپ) · s3cmd (آپلود به Arvan) · docker + docker compose v2
```

⚠️ **`s3cmd` نه `aws`.** اوبونتو ۲۴٫۰۴ بسته‌ی `awscli` را حذف کرده و نصب رسمی
AWS به بین‌الملل نیاز دارد که این سرور ندارد.

### فرانت پروداکشن مسیر نسبی می‌زند، نه دامنه

`VITE_API_URL` **در زمان build** داخل باندل می‌نشیند. اگر آنجا دامنه بنویسیم،
ایمیج به `app.dofixo.ir` گره می‌خورد و هر تغییر دامنه یک build دوباره است.

`src/api` حالا با `import.meta.env.PROD` تشخیص می‌دهد و در پروداکشن `/api`
می‌زند. Caddy مسیریابی می‌کند (۷.۴).

⚠️ دام: نمی‌شود `VITE_API_URL=""` گذاشت تا نسبی شود — `||` رشته‌ی خالی را
falsy می‌بیند و به `localhost:5001` برمی‌گردد، یعنی اپ پروداکشن به لپ‌تاپ
کاربر درخواست می‌زند.

**در پروداکشن CORS منتفی است** (یک origin) و کوکی `SameSite=Strict` دیگر
cross-origin نیست.

⚠️ `index.html` نباید کش شود. اگر بشود، مرورگر به باندل‌های حذف‌شده اشاره
می‌کند: صفحه‌ی سفید بعد از هر استقرار، بدون هیچ چیزی در لاگ.

⚠️ بدون `try_files ... /index.html`، رفرش روی `/devices` یعنی ۴۰۴.

### ساختار مخزن

```
backend/
├── prisma/    schema.prisma (۳۰ مدل) · seed.ts · rls-check.sql · ۱۴ مهاجرت
├── src/
│   ├── lib/          prisma · storage · workspaceContext · sms · zibal · imageProfile
│   ├── controllers/  ۱۵ کنترلر
│   ├── routes/       همه TypeScript جز index.js
│   ├── middleware/   auth · authorize · requestContext · validate · subscription
│   ├── scripts/      subscriptionCron.ts (cron شبانه، §۸.۷)
│   ├── schemas/ · types/
│   ├── utils/        subscription · pricing · referral · referralCode
│   │                 subscriptionSchedule · subscriptionJob · workspaceDeletion
│   │                 jalali · orderId · export/ · …
│   └── __tests__/    ۴۲ فایل (~۵۵۷ تست) + integration/ (۱۰ فایل، ~۱۲۸ تست)
├── Dockerfile        پروداکشن، multi-stage
└── Dockerfile.dev

frontend/
├── src/   api/ · context/ (۴) · utils/ (۳) · types/ (۲)
│          components/ (۲۹) · pages/ (۱۷) · همه TypeScript
│          فاز ۸: SubscriptionContext · SubscriptionBanner · PaymentReceipt
│                 Subscription · PaymentCallback · Referral
├── Dockerfile        پروداکشن، nginx-unprivileged
├── Dockerfile.dev
└── nginx.conf

ops/
├── backup-database.sh    بکاپ شبانه (cron)
├── extract-workspace.sh  استخراج یک کارگاه از بکاپ
├── reset-password.sh     بازیابی دسترسی کاربر
├── subscription-cron.sh  اجرای شبانه‌ی اشتراک (cron)
├── restore-database.md · restore-workspace.md · reset-password.md
├── subscription-cron.md
└── backup.env.example

docker-compose.yml · docker-compose.prod.yml · Caddyfile · .env.prod.example
```

**دو فایل `.js` در بک‌اند:** `routes/index.js` و `utils/persianToEnglish.js`.

### مهاجرت‌ها

| نام                                     | کارش                                             |
| --------------------------------------- | ------------------------------------------------ |
| `20260809073159_init_multitenant`       | اسکیمای اولیه با `workspaceId`                   |
| `20260810095035_invoice_counters`       | سه ستون شمارنده روی `workspaces`                 |
| `20260810120000_rls_policies`           | نقش `dofixo_app` + policyها + grantها            |
| `20260810140000_login_lookup`           | `app_login_lookup` (دریچه‌ی ۱)                   |
| `20260810150000_create_workspace`       | `app_create_workspace` (دریچه‌ی ۲)               |
| `20260812074748_refresh_tokens`         | جدول + policy + `app_refresh_lookup` (دریچه‌ی ۳) |
| `20260819031426_export_status`          | `status` · `filepath` · `error` روی `backups`    |
| `20260824092832_otp_codes`              | جدول OTP + policy `USING (true)`                 |
| `20260827085131_device_image_thumbnail` | ستون `thumbnail_path`                            |
| `20260828000000_seed_roles`             | سه نقش به‌عنوان داده‌ی مرجع (دستی)               |
| `20260830031113_subscriptions`          | هشت مدل فاز ۸ + ۷ policy + سه پلن + backfill     |
| `20260830060000_trial_length_in_app`    | `app_create_workspace` دیگر انقضا نمی‌دهد (دستی) |
| `20260830070000_discount_use_count`     | policy دوم برای شمارش سراسری کد تخفیف (دستی)     |
| `20260831192037_payment_discount_code`  | ستون `discount_code_id` روی `payments`           |
| `20260830053153_subscriptions`          | هشت مدل فاز ۸ + ۷ policy + سه پلن + backfill     |

⚠️ **ترتیب اجرا الفبایی است، نه زمانی.** هر مهاجرت دستی جدید باید timestamp
بزرگ‌تر از `20260831192037` بگیرد.

---

## ۴. تصمیمات معماری و دلیلشان

مهم‌ترین بخش سند. هر تصمیم دلیلی دارد که اگر ندانید، ممکن است تصادفاً برش گردانید.

### زیرساخت: یک سرور، نه دو تا

**یک VPS در ایران** همه‌چیز را اجرا می‌کند: بک‌اند، فرانت، Postgres و Caddy،
به‌صورت چهار کانتینر با volume جدا برای دیتابیس.

**چرا نه دیتابیس مدیریت‌شده:** پارس‌پک DBaaS مستقل ندارد، ولی PaaS اش PostgreSQL
به‌عنوان اپلیکیشن آماده با بکاپ زمان‌بندی‌شده می‌دهد و پشتیبانی تأیید کرد کاربرش
superuser است. رد شد چون با ۳ هسته و ۴ گیگ رم حدود ۱۶٫۷ میلیون تومان در سال است —
تقریباً همان قیمت یک سرور کامل که هم دیتابیس را جا می‌دهد و هم اپ را.

**چرا نه دو سرور:** طراحی RLS ما **هر کوئری را دو رفت‌وبرگشت** می‌کند
(`set_config` به‌علاوه‌ی خود کوئری). روی یک ماشین هر رفت‌وبرگشت ~۰٫۱ میلی‌ثانیه
است؛ بین دو سرویس ~۰٫۵ تا ۱. جدا کردن، تأخیر را پنج تا ده برابر می‌کند بدون هیچ
سودی در این مقیاس. جدا کردن بعداً فقط عوض کردن `DATABASE_URL` است؛ الان یعنی دو
سیستم‌عامل برای آپدیت و یک شبکه‌ی خصوصی برای پیکربندی.

**بار واقعی:** با ۵۰۰ کارگاه و ۵۰ کاربر همزمان، حدود ۵ درخواست در ثانیه — کمتر از
۵ درصد ظرفیت. فرانت بعد از build فقط فایل استاتیک است. حجم دیتابیس سال‌ها زیر ۱۰
گیگ می‌ماند چون عکس‌ها روی Arvan هستند.

**`shared_buffers` روی ۲ گیگ است** (پیش‌فرض ایمیج ۱۲۸ مگابایت بود)، به‌علاوه‌ی
`effective_cache_size=5GB` که تخصیص نیست — به planner می‌گوید چقدر از page cache
سیستم‌عامل را می‌تواند فرض کند، و همین است که index scan را ترجیح می‌دهد.

### شکل پاسخ API — آنچه تایپ‌ها ثبت کردند

`types/api.ts` از روی کنترلرها نوشته شد نه حدس. سه ناهمگونی عمدی که حالا
مکتوب‌اند:

- بیشتر API **`snake_case`** است؛ `items` و `categories` **`camelCase`**
- **جز `sell_price`** که در بدنه‌ی ساخت و ویرایش کالا snake_case است
- **جز دو endpoint کالا** — `transactions` و `search/for-invoice` — که
  snake_case می‌دهند برخلاف همسایه‌هایشان

و سه endpoint که پاکت صفحه‌بندی **ندارند** و آرایه‌ی خام می‌دهند:
`GET /personnel` · `GET /categories` · `GET /services`

**فیلدهای دستگاه روی فاکتور فروش پخش می‌شوند نه تودرتو**، و وقتی فاکتور به
دستگاهی وصل نباشد کلاً غایب‌اند. `serial_number` فقط در تک‌فاکتور می‌آید.

### تنانسی

**دیتابیس مشترک، اسکیمای مشترک.** ایزولاسیون از طریق `workspaceId` روی هر جدول
tenant-scoped. **دو لایه:** فیلتر اپلیکیشنی + RLS. هیچ‌کدام حذف نشود.

### دو اتصال دیتابیس، عمداً

| متغیر              | نقش          | مصرف                 |
| ------------------ | ------------ | -------------------- |
| `DATABASE_URL`     | `dofixo`     | فقط migration و seed |
| `DATABASE_URL_APP` | `dofixo_app` | اپ در حال اجرا       |

در Postgres مالک جدول RLS را **دور می‌زند**. `dofixo_app` مالک هیچ جدولی نیست.
⚠️ `lib/prisma.ts` عمداً **fallback ندارد** — fallback یعنی یک `.env` ناقص
بی‌سروصدا اپ را به اتصال superuser برمی‌گرداند.

⚠️ **در پروداکشن `DATABASE_URL` فقط در سرویس `migrate` وجود دارد**، که profile
دارد و هرگز کنار اپ اجرا نمی‌شود. اگر آن credential در محیط process ای باشد که
درخواست جواب می‌دهد، کل فاز ۲ با خواندن `process.env` خنثی می‌شود.

### زنجیره‌ی `app.workspace_id`

```
requestContext (middleware) → AsyncLocalStorage باز می‌شود (خالی)
authenticate                → setContextWorkspaceId(payload.workspaceId)
Prisma extension            → currentWorkspaceId() را می‌خواند
                            → set_config('app.workspace_id', id, TRUE)
```

**اگر context نباشد، extension خطا پرتاب می‌کند** — نه اینکه بگذارد RLS ساکت صفر
ردیف برگرداند.

### ⚠️ multer زنجیره را قطع می‌کند

`AsyncLocalStorage` از promise عبور می‌کند ولی **از رویداد stream نه**. multer
بدنه‌ی `multipart` را با busboy می‌خواند — نتیجه اینکه `req.user` سالم می‌ماند ولی
**هیچ store ای وجود ندارد**، نه اینکه store خالی باشد.

`restoreWorkspaceContext` باید **بعد از** multer روی هر مسیری که فایل می‌پذیرد
بنشیند. چون store دیگر وجود ندارد، این تابع `storage.run(...)` می‌زند نه
`setContextWorkspaceId(...)`.

### دو helper تراکنش

```typescript
// ❌ هرگز
await prisma.$transaction(async (tx) => { ... });

// ✅ تراکنش معمولی
await runInWorkspaceTransaction(workspaceId, async (tx) => { ... });

// ✅ ثبت‌نام: کارگاه هنوز وجود ندارد
await runInNewWorkspaceTransaction(name, async (tx, workspaceId) => { ... });
```

عملیات داخل تراکنش دوباره وارد extension می‌شود و تراکنش دومی روی اتصال دوم باز
می‌کند؛ `set_config` آنجا می‌نشیند و کار اصلی بدون context ادامه می‌دهد.

### سه دریچه‌ی `SECURITY DEFINER`

هر سه وجود دارند چون فراخوان هنوز workspace ندارد. هیچ‌کدام نباید گشاد شوند.

| تابع                   | کِی                         | چه برمی‌گرداند                         |
| ---------------------- | --------------------------- | -------------------------------------- |
| `app_login_lookup`     | ورود، قبل از احراز          | id، workspace، هش رمز، وضعیت فعال      |
| `app_create_workspace` | ثبت‌نام، کارگاه هنوز نیست   | فقط `id` کارگاه تازه                   |
| `app_refresh_lookup`   | refresh، access token منقضی | id ردیف، user، workspace، انقضا، ابطال |

⚠️ هر سه `SET search_path = public` دارند و `EXECUTE` از `PUBLIC` گرفته شده.

⚠️ **در دیتابیس چهار تابع `app_*` هست، نه سه.** چهارمی `app_current_workspace_id`
است که دریچه نیست — policyها از آن استفاده می‌کنند. موقع شمارش در بازیابی،
عدد درست **۴** است.

### احراز هویت

**access token:** JWT، ۱۵ دقیقه، در **حافظه‌ی صفحه** نه localStorage.

**refresh token:** ۳۰ روز، راز تصادفی ۳۲ بایتی — **نه JWT**، چون JWT وسوسه‌ی
اعتبارسنجی بدون دیتابیس را می‌آورد و ابطال‌پذیری را از بین می‌برد. در دیتابیس فقط
هش SHA-256.

**کوکی:** `httpOnly`، `SameSite=Strict`، `Path=/api/auth`، `Secure` فقط در پروداکشن.

**چرخش با تشخیص سرقت:** ارائه‌ی دوباره‌ی توکن **باطل‌شده** یعنی نسخه‌ای در گردش
است، پس همه‌ی session های آن کاربر بسته می‌شوند.

**logout حذف می‌کند، نه ابطال.** ابطال سیگنال سرقت است؛ پایان دادن به session خودت
سیگنالی ندارد.

**سمت فرانت:** رهگیر Axios یک ۴۰۱ را بعد از refresh دوباره تلاش می‌کند، و **۴۰۱ های
همزمان یک refresh مشترک دارند** — وگرنه دومی توکن تازه‌چرخیده را می‌فرستد و سرور آن
را سرقت می‌خواند.

**نگهبان `res.headersSent`:** کلاینتی که وسط درخواست می‌رود، باعث می‌شد کد روی پاسخ
بسته بنویسد و ۵۰۰ لاگ شود برای چیزی که هرگز شکست نخورده بود.

### سخت‌سازی پروداکشن (۷.۳الف)

سه پیش‌فرض که برای توسعه درست بودند و برای یک دامنه‌ی عمومی خطرناک.

**سه چیز به‌جای اجرای ناامن، بالا نمی‌آیند:** `DATABASE_URL_APP`، `SMS_API_KEY`
و `JWT_SECRET`. آخری تا ۷.۳الف به رشته‌ای در همین مخزن fallback می‌کرد — با
متغیر خالی، هر کسی می‌توانست توکنی با هر `workspaceId` بسازد و RLS با همان
عدد جعلی scope می‌کرد.

**`trust proxy` عدد است نه بولی.** بولی یعنی اعتماد به کل زنجیره‌ی
`X-Forwarded-For` که کلاینت می‌نویسد — هر درخواست یک IP تازه، و سقف per-IP
در `send-otp` بی‌اثر. یک Caddy = یک hop = `1`.

**CORS فقط خارج از پروداکشن ثبت می‌شود.** بعد از ۷.۲ فرانت `/api` نسبی می‌زند
و یک origin است، پس `origin: true` با credentials کاری ندارد جز بازتاب هر
origin ای که بپرسد.

### نام کاربری = شماره موبایل

موبایل ایران، ۱۱ رقم با `09`. normalize یکسان در ثبت‌نام، ورود **و ساخت پرسنل**
(`phoneSchema` مشترک): ارقام فارسی و عربی‌هندی، جداکننده‌ها، `+98` و `0098`.

ستون `users.phone` حالا «شماره تماس دیگر» است (اختیاری).

### Object storage

**صندوقچه‌ی خصوصی + presigned URL** (۱۵ دقیقه)، امضا به‌ازای هر درخواست، **بعد از**
اینکه ردیف با workspace فیلتر شد. object storage هیچ RLS ای ندارد.

**کلیدها مستأجر را حمل می‌کنند:**

```
workspaces/{id}/devices/{deviceId}/{uuid}.webp
workspaces/{id}/devices/{deviceId}/thumbs/{uuid}.webp
workspaces/{id}/settings/{type}-{uuid}.webp
exports/{id}/{timestamp}-export.zip
```

**`filepath` و `thumbnailPath` در دیتابیس همان کلید کامل‌اند** و مستقیم امضا
می‌شوند، نه اینکه بازسازی شوند — تا اگر روزی ساختار کلید عوض شد یا کارگاه با
شناسه‌ی جدید بازیابی شد، عکس‌ها پیدا بمانند.

**`lib/storage.ts` تنها ماژولی است که SDK را می‌شناسد.**

**خطای حذف لاگ می‌شود، پرتاب نمی‌شود.** **ترتیب حذف:** اول شیء، بعد ردیف.

**باکت‌ها:** `dofixo-prod` (پروداکشن) و `reza-app-test-1` (توسعه). هر دو خصوصی.

### پردازش تصویر (۷.۰)

`lib/imageProfile.ts` تنها جایی است که اعداد در آن‌اند: **۳۴۰۰px، q85**،
به‌علاوه‌ی thumbnail ۴۸۰px با q75.

عدد از اندازه‌گیری آمد نه قاعده‌ی سرانگشتی — پانزده عکس واقعی برد (۸۰۰۰×۶۰۰۰،
میانگین ۷٫۸ مگابایت خام) در پنج عرض و پنج کیفیت:

|      | q92  | q85  | q78  |
| ---- | ---- | ---- | ---- |
| اصلی | ۳٫۹۰ | ۲٫۲۰ | ۱٫۴۲ |
| ۴۰۰۰ | ۱٫۶۲ | ۰٫۸۱ | ۰٫۴۸ |
| ۳۴۰۰ | ۱٫۲۳ | ۰٫۶۰ | ۰٫۳۶ |
| ۲۸۰۰ | ۰٫۸۶ | ۰٫۴۱ | ۰٫۲۴ |

**۳۴۰۰ از روی خروجی انتخاب شد نه از روی جدول:** پایین‌تر، نوشته‌ی روی IC های
کوچک خوانده نمی‌شود — که کل دلیل وجود این عکس‌هاست. ۶٫۵ برابر کوچک‌تر از قبل.

⚠️ **رزولوشن و quality دو دکمه‌ی مستقل‌اند.** جزئیات برد از رزولوشن می‌آید، نه
quality. ۹۲ به ۸۵ حجم را نصف می‌کند بدون یک پیکسل کمتر.

**`.rotate()` یافته‌ی اصلی بود.** یازده از پانزده عکس جهت EXIF غیرپیش‌فرض
داشتند. باگ فعال بود، نه احتمالی. تگ را هم حذف می‌کند، که مختصات GPS دوربین
را پاک می‌کند.

**thumbnail چون مودال دستگاه همه‌ی عکس‌ها را یکجا در شبکه‌ای حداکثر ۱۲۸ پیکسلی
رندر می‌کند** — و هر بار دوباره، چون هر presigned URL یکتاست و مرورگر هیچ‌وقت
کش نمی‌کند. ۴٪ storage بیشتر، حدود صدم ترافیک.

**سقف multer ۲۵ مگابایت است.** حجم ذخیره‌شده را resize تعیین می‌کند نه ورودی.

هزینه‌ی سه‌ساله‌ی یک کارگاه: از ~۱٫۳ میلیون به ~۲۰۰ هزار تومان.

**نرخ آروان:** ۵۰۰ گیگ ثابت ۱٬۶۰۰٬۰۰۰ + مازاد گیگی ۳٬۰۰۰ ماهانه. **خطی است،
بدون پله** — پس باکت دوم و ستون `bucket` هرگز لازم نیست.

### بکاپ

**`ops/backup-database.sh` از cron روی هاست اجرا می‌شود**، بیرون از کانتینرها —
اپی که خراب شده نباید مسئول بکاپ گرفتن از خودش هم باشد.

**همه در یک pipeline:** `pg_dump | gzip | age`. dump خام هرگز روی دیسک نمی‌نشیند،
پس نسخه‌ی رمزنشده‌ی داده‌ی همه‌ی مشتری‌ها هیچ‌وقت در `/tmp` نیست.

**رمزنگاری نامتقارن با `age`.** فقط کلید عمومی روی سرور است؛ کلید خصوصی در password
manager و یک نسخه‌ی آفلاین. سروری که نفوذ شده می‌تواند بکاپ بنویسد ولی نمی‌تواند
تاریخچه‌اش را بخواند.

⚠️ **گم شدن کلید خصوصی یعنی همه‌ی بکاپ‌ها برای همیشه غیرقابل بازیابی‌اند.**

**نگهداری در سه پوشه:** `daily` (۷)، `weekly` (۴)، `monthly` (۳) — هرکدام مستقل
هرس می‌شوند، پس باگ در یکی نمی‌تواند بقیه را پاک کند. هر اجرا حداکثر ۵ فایل حذف
می‌کند، تا فهرست اشتباه به‌جای فاجعه یک معما شود.

**مسیرهای compose از `backup.env` می‌آیند** (`COMPOSE_DIR` · `COMPOSE_FILE` ·
`COMPOSE_ENV_FILE`)، نه از محل خود اسکریپت — چون روی سرور مخزنی نیست و فایل
compose نامش `docker-compose.yml` نیست.

**اسنپ‌شات‌های VPS جایگزین این نیستند:** crash-consistent اند نه
application-consistent، بازیابی‌شان همه‌یا‌هیچ است، و نمی‌شود یک کارگاه را از
دلشان بیرون کشید.

### بازیابی یک کارگاه

`ops/extract-workspace.sh` dump کامل را به یک مستأجر تقلیل می‌دهد: **همه‌ی
کارگاه‌های دیگر را حذف می‌کند** و `ON DELETE CASCADE` بقیه را خالی می‌کند. بسیار
کم‌خطاتر از پیمودن ۱۹ جدول به ترتیب وابستگی.

بعد **همه‌ی شناسه‌ها را با یک عدد ثابت (۱٬۰۰۰٬۰۰۰) جابه‌جا می‌کند**. کلیدهای
خارجی خودشان دنبال می‌آیند، چون Prisma همه را `ON UPDATE CASCADE` اعلام می‌کند.

**کارگاه با شناسه‌ی تازه برمی‌گردد.** شناسه‌ها از sequenceهای مشترک می‌آیند، پس
برگرداندن شناسه‌های قدیمی با ردیف‌هایی که کارگاه‌های دیگر ساخته‌اند تداخل می‌کند.
هزینه‌اش این است که کاربران آن کارگاه باید دوباره وارد شوند.

⚠️ **بعد از بارگذاری حتماً sequenceها را جلو ببرید**، وگرنه کارگاه‌های دیگر به‌مرور
به شناسه‌های بازیابی‌شده می‌رسند.

**refresh tokenها بازیابی نمی‌شوند:** زنده کردن session هایی که منطق چرخش عمداً
باطلشان کرده بود، تشخیص سرقت را خنثی می‌کند.

**کلیدهای object storage پیشوند قدیمی را نگه می‌دارند.** بی‌ضرر است چون `filepath`
کلید کامل را دارد، ولی یعنی پیشوند دیگر شناسه‌ی معتبری برای «اشیای این کارگاه» نیست.

### بازیابی دسترسی کاربر

`ops/reset-password.sh` رمز تصادفی می‌سازد و همه‌ی session های آن کاربر را حذف
می‌کند.

**اسکریپت است نه راهنما، برخلاف آنچه روادمپ می‌گفت.** «رمزم را فراموش کرده‌ام» با
۵۰۰ کارگاه هفتگی است نه یک فاجعه‌ی نادر، و روال دستی یعنی هر بار یک `UPDATE` روی
`users` — که یک شب خسته با `WHERE` فراموش‌شده، رمز همه‌ی کاربران پلتفرم را عوض
می‌کند.

⚠️ حالا که OTP.5 هست، این اسکریپت **استثناست نه مسیر اصلی**: فقط برای کسی که
خود شماره را از دست داده.

⚠️ **خطرناک‌ترین بخش، احراز هویت تلفنی است نه اجرای اسکریپت.** تنها قدمی که واقعاً
چیزی ثابت می‌کند، **تماس گرفتن با شماره‌ی ثبت‌شده** است، نه جواب دادن به تماس
ورودی. `ops/reset-password.md` چک‌لیستش را دارد.

### شماره‌گذاری فاکتور

سه شمارنده روی ردیف `Workspace`. قالب `PUR-0001`، `SAL-0001`، `REP-0001`.
`seq = seq + 1` قفل ردیف می‌گیرد (با ده درخواست موازی تست شده).

پیشوند از تنظیمات نمی‌آید: شماره داده‌ی حسابداری است و باید خسته‌کننده بماند.

### مجوزهای نقش اپ

| جدول                 | مجوز                          | چرا                       |
| -------------------- | ----------------------------- | ------------------------- |
| `roles`              | فقط `SELECT`                  | داده‌ی مرجع               |
| `workspaces`         | `SELECT` + `UPDATE`           | شمارنده‌ها؛ ساخت از دریچه |
| `_prisma_migrations` | هیچ                           | داده‌ی اپلیکیشن نیست      |
| بقیه                 | `SELECT/INSERT/UPDATE/DELETE` | —                         |

`ALTER DEFAULT PRIVILEGES` جدول‌های آینده را پوشش می‌دهد. **ولی RLS منتقل نمی‌شود**
— هر مهاجرتی که جدول با `workspace_id` بسازد باید RLS و policy را در همان مهاجرت
اضافه کند.

### `roles` در مهاجرت است، نه seed

`20260828000000_seed_roles`. داده‌ی مرجع است نه seed: سه ردیف که هیچ‌کس
ویرایششان نمی‌کند و نقش اپ فقط `SELECT` رویشان دارد.

راه‌اندازی پروداکشن حالا فقط `migrate deploy` است. `seed.ts` همچنان یک
«کارگاه پیش‌فرض» و سوپرادمین می‌سازد — که برای توسعه درست است و در پروداکشن
هرگز اجرا نمی‌شود.

⚠️ fixture های یکپارچگی هنوز `upsert` می‌کنند، چون `truncateAll()` بین هر تست
همه‌ی جدول‌ها از جمله `roles` را خالی می‌کند. تکرار نیست.

### کلید export از پیشوند کارگاه بیرون آمد

`exports/{workspaceId}/{file}` به‌جای `workspaces/{id}/exports/{file}`.

**علتش پنل آروان است:** قانون چرخه‌عمر فقط پیشوند ساده می‌گیرد، نه wildcard.
با ساختار قبلی تنها پیشوند ممکن `workspaces/` بود — که عکس‌های همه‌ی
کارگاه‌ها را هم بعد از سی روز پاک می‌کرد.

اصل «کلید مستأجر را حمل می‌کند» نقض نشده: workspace هنوز در کلید است، یک سطح
پایین‌تر. آنچه واقعاً محافظت می‌کند بررسی اپلیکیشن قبل از امضاست، نه شکل کلید
— همان‌طور که `extract-workspace.sh` قبلاً نشان داد.

سود جانبی: حجم export ها با یک نگاه به پیشوند `exports/` معلوم می‌شود.

### اشتراک: یک تابع، یک ستون، یک ساعت

**`extendSubscription` تنها جایی است که `expiresAt` عوض می‌شود.** آزمایشی،
پرداخت، پاداش رفرال و اصلاح دستی — هر چهار مسیر از آن رد می‌شوند و هر کدام یک
`SubscriptionEvent` جا می‌گذارند. سه تا از آن چهار مسیر پرداخت نیستند، پس
جدول `payments` به‌تنهایی هرگز نمی‌توانست به «چرا انقضای من این تاریخ است»
جواب بدهد.

**از `max(امروز، انقضا)` حساب می‌کند.** خرید در ماه دوم یک اشتراک سه‌ماهه به
باقی‌مانده اضافه می‌شود نه جایگزینش؛ و پاداش رفرالِ کارگاهی که هفته‌ی پیش
منقضی شده، در گذشته خرج نمی‌شود.

⚠️ **خواندن با `SELECT ... FOR UPDATE` است، نه `findUnique`.** دو پرداخت که
همزمان تأیید شوند وگرنه هر دو انقضای قدیمی را می‌خوانند و یکی دور ریخته
می‌شود. قرار گذاشتیم هر دو خرید حساب شوند، و قفل ردیف همان چیزی است که این
را درست می‌کند — همان استدلال شمارنده‌ی فاکتور.

**`Workspace.status` نوشته می‌شود ولی هرگز مبنای مجوز نیست.** یک ستون
ذخیره‌شده فقط به‌اندازه‌ی آخرین اجرای cron تازه است، و شبی که آن job اجرا
نشود نباید به هر کارگاه منقضی یک روز نوشتن اضافه بدهد. **مجوز حق ندارد به
موفق بودن یک job وابسته باشد.**

### نگهبان فقط-خواندنی (۸.۳)

**۴۰۲ نه ۴۰۳.** تکنسینی که به مسیر ادمین می‌خورد ممنوع است؛ کارگاهی که
اشتراکش تمام شده ممنوع نیست. فرانت باید این دو را از هم تشخیص بدهد، و کد
وضعیت از رشته‌ی پیام قابل‌اتکاتر است. بدنه یک `code` هم دارد.

⚠️ **از انتهای `authenticate` صدا زده می‌شود، نه در سیزده روت‌فایل.** این
دقیقاً شکل بدهی ۳۱ است: پنج فایل `atLeast("admin")` نداشتند و هیچ تستی قرمز
نشد، چون نبودِ نگهبان عیناً شبیه نبودِ نگهبان است. یک نقطه‌ی اتصال فراموش
نمی‌شود — و سوئیت یکپارچگی همین را ثابت کرد: ۲۷ تست قدیمی به‌محض اینکه
fixture آزمایشی نداد، ۴۰۲ گرفتند.

**خواندن‌ها دست‌نخورده رد می‌شوند و هیچ کوئری‌ای نمی‌زنند.** کارگاه منقضی هر
صفحه‌ای که داشت را می‌بیند و خروجی‌های قبلی‌اش را دانلود می‌کند.

| مسیر                          | در حالت منقضی |
| ----------------------------- | ------------- |
| هر `GET` · `HEAD` · `OPTIONS` | باز           |
| `PUT /auth/change-password`   | باز           |
| `/subscription/*`             | باز           |
| `GET /exports` و دانلودش      | باز           |
| `POST /exports`               | بسته          |
| تنظیمات (`PUT` و `POST`)      | بسته          |
| بقیه‌ی نوشتن‌ها               | بسته          |

**دانلود خروجی قبلی باز است، ساخت خروجی تازه نه.** کارگاهی که پول نمی‌دهد
باید بتواند داده‌اش را ببرد؛ نگه داشتنش هم غلط است هم در بازار کوچک
تعمیرکاران گران‌تر از CPU ای که صرفه‌جویی می‌کند.

⚠️ **فهرست مسیرهای باز با `baseUrl + path` سنجیده می‌شود، نه `path` تنها.**
داخل روتری که دو بار mount شده، `req.path` نسبی است:
`/api/auth/change-password` به‌شکل `/change-password` می‌رسد. مطابقت با آن
تنها یعنی هر روتری که روزی مسیری به این نام داشته باشد بی‌صدا مستثنا شود.
تستی هست که همین خط را نگه می‌دارد.

**`neverExpires` هیچ مسیر API ندارد** و هیچ اعتبارسنجی Zod ای هم. فقط با
psql ست می‌شود — چیزی که راه رسیدن نداشته باشد، سوءاستفاده هم نمی‌شود.

### قیمت‌گذاری (۸.۴)

**کلاینت کد پلن می‌فرستد، هرگز مبلغ.** قیمتی که از مرورگر بیاید، قیمتی است که
مشتری انتخاب کرده.

**تخفیف‌ها جمع نمی‌شوند.** دو کد ۵۰٪ روی هم یعنی اشتراک مجانی، و روزی که
اتفاق بیفتد از ترکیبی خواهد بود که کسی تصورش را نکرده. تساوی به نفع رفرال
تمام می‌شود: پول یکی است و کد مشتری برای دفعه‌ی بعد دست‌نخورده می‌ماند.

**تخفیف گرد می‌شود، نه مبلغ نهایی.** اگر مبلغ را گرد کنیم، «قیمت منهای
تخفیف» با آنچه واقعاً گرفته شده چند صد ریال اختلاف پیدا می‌کند — همان چیزی
که صاحب تعمیرگاه روی رسید می‌بیند و هیچ‌کس نمی‌تواند توضیحش بدهد.

**کف ۱۰٬۰۰۰ ریال:** زیبال زیر هزار ریال را رد می‌کند (کد ۱۰۵) و صفر اصلاً
تراکنش نیست. در آن حالت رسید عمداً جمع نمی‌زند — عدد ناجور همان چیزی است که
کد تخفیف اشتباه را دیدنی می‌کند.

**`quotePrice` تابع خالص است.** وجود کد، انقضا، سقف استفاده و اینکه این
کارگاه قبلاً خرجش کرده یا نه، سؤال‌های دیتابیس‌اند و مال ۸.۵.

### زیبال (۸.۵)

**روش عادی، نه `lazy`.** در lazy اگر ظرف بیست دقیقه تأیید نفرستیم پول خودکار
برمی‌گردد — که cron روزانه هرگز به آن نمی‌رسد — و callback اش `POST` با JSON
است که هیچ صفحه‌ی فرانتی نمی‌تواند دریافتش کند.

**کد نتیجه‌ی `201` («قبلاً تأیید شده») موفقیت است، نه خطا.** این همان چیزی
است که رفرش کردن صفحه‌ی بازگشت می‌سازد. `newlyVerified` تفکیکش می‌کند و جلوی
تمدید دوباره را می‌گیرد.

⚠️ **مبلغ برگشتی با ردیف پرداخت مقایسه می‌شود.** تمدید روی اختلاف یعنی فروش
اشتراک به هر مبلغی که مشتری تصمیم گرفته.

**ردیف پرداخت قبل از صدا زدن زیبال نوشته می‌شود**، تا درگاهی که دیر یا اصلاً
جواب نمی‌دهد، ردی بگذارد. قیمت و مدت روی همان ردیف کپی می‌شوند — با تورم
ایران، رسید پارسال که با قیمت امسال رندر شود رسید نیست.

⚠️ **`ZIBAL_MERCHANT` و `APP_URL` در زمان import خوانده می‌شوند.** اولی در
پروداکشن اگر هنوز `ZIBAL` باشد اپ را بالا نمی‌آورد: با آن حساب همه‌چیز کار
می‌کند و هیچ پولی جابه‌جا نمی‌شود، یعنی اپ کاملاً سالم به‌نظر می‌رسد در حالی
که برای پرداخت‌هایی که هرگز انجام نشده اشتراک فعال می‌کند. دومی callback را
می‌سازد که زیبال دامنه‌ی غیرثبت‌شده‌اش را رد می‌کند (کد ۱۰۶).

🔴 **checkout به `Referrer-Policy` در Caddyfile وابسته است.** زیبال روی
`/start/{trackId}` هدر `Referer` منطبق با دامنه‌ی ثبت‌شده می‌خواهد. مقدار
فعلی `strict-origin-when-cross-origin` درست است؛ با `no-referrer` درگاه
اصلاً باز نمی‌شود و **هیچ چیزی در لاگ ما ثبت نمی‌شود**. آن خط را عوض نکن.

**فرانت باید `window.location` بزند، نه `fetch`** — فقط ناوبری واقعی
`Referer` می‌فرستد.

### دفتر مالی پاک نمی‌شود

`payments` و `subscription_events` و `discount_code_uses` هیچ‌کدام `DELETE`
برای نقش اپ ندارند. دفتری که پاک شود دفتر نیست، و ردیف مصرف کد تخفیف **خودِ
قاعده‌ی «یک بار برای هر کارگاه»** است.

**کارگاه هرگز حذف نمی‌شود.** حذف داده در ۸.۷ ردیف `Workspace` را به‌عنوان
سنگ قبر نگه می‌دارد (`deletedAt` و `status = deleted`). دلیلش دو چیز است:
نقش اپ عمداً `DELETE` روی `workspaces` ندارد، پس cron باید با credential
مالک اجرا می‌شد؛ و با ماندن ردیف، `payments` کلید خارجی سالم و غیر-null نگه
می‌دارد.

⚠️ **بهایش این است که ترتیب حذف را باید خودمان بنویسیم** — بدون cascade،
`Restrict` جلوی حذف `items` را می‌گیرد تا وقتی `purchase_invoice_items`
هست. یک تابع صریح و تست‌پذیر، در ۸.۷.

### رفرال و RLS — سه مسئله‌ی جدا

| مسئله                                    | راه‌حل                                |
| ---------------------------------------- | ------------------------------------- |
| ردیف رفرال دو کارگاه دارد                | policy دوطرفه، تنها مورد در کل اسکیما |
| پاداش روی کارگاه دیگری نوشته می‌شود      | `runWithWorkspace()` (۸.۶)            |
| ثبت‌نام باید کد کارگاه ناشناسی را بخواند | policy خواندن باز / نوشتن محدود       |

⚠️ **`ReferralCode` به `UNSCOPED_MODELS` اضافه نشد.** موقع ثبت‌نام context
باز است — فقط روی کارگاه _اشتباه_. یعنی مسئله‌ی policy است نه مسئله‌ی
«context نداریم»، و با policy جواب داده شد. **آن مجموعه هنوز دقیقاً یک عضو
دارد.**

**شناسه‌ی کارگاه دعوت‌کننده از دیتابیس خودمان می‌آید، نه از کلاینت.** همان
تفکیکی که `RULES.md` می‌گذارد.

### رفرال — پاداش روی کارگاه دیگری نوشته می‌شود

**`linkReferral` در تراکنش ثبت‌نام است**، پس ثبت‌نامی که بعدش شکست بخورد
رابطه را با خودش می‌برد. **کد اشتباه ثبت‌نام را نمی‌شکند** — بیرون کردن کسی
در آخرین قدم فرم به‌خاطر یک حرف اشتباه، یک مشتری هزینه دارد — ولی پاسخ
`referral_applied` را برمی‌گرداند تا فرم بگوید شناسایی نشد.

**`rewardReferrer` بعد از `settlePayment` است، نه داخلش.** پاداش روی کارگاه
_دیگری_ نوشته می‌شود و context خودش را می‌خواهد؛ تو در تراکنش پرداخت گذاشتنش
دقیقاً همان شکلی است که `runInWorkspaceTransaction` برای پرهیز از آن نوشته
شد.

⚠️ **خطاهایش را می‌بلعد**، مثل `deleteObject`. مشتری‌ای که تازه پول داده نباید
۵۰۰ ببیند چون سی روز کس دیگری نوشته نشد.

**ادعا قبل از پاداش گرفته می‌شود** و اگر پاداش شکست بخورد پس داده می‌شود —
وگرنه رفرال «پرداخت‌شده» می‌خواند در حالی که هیچ روزی اضافه نشده، و هیچ‌چیز
دوباره تلاش نمی‌کند. تنها حالتی که کسی بی‌صدا ضرر می‌کند.

**فقط اولین خرید.** تمدید یعنی خود کارگاه دارد برای خودش پول می‌دهد، و پاداش
دادن به آن یک معرفی را به مستمری تبدیل می‌کند.

### cron شبانه (۸.۷)

**روی هاست، مثل `backup-database.sh`** و به همان دلیل: اپی که از کار افتاده
نباید همان چیزی باشد که هشدار پایان اشتراک را هم می‌فرستد. اسکریپت bash فقط
پوسته است؛ کار داخل کانتینر و در TypeScript انجام می‌شود، چون حذف مرتب بیست
جدول و حساب روز انقضا در bash نوشتنی نیست و در TypeScript تست دارد.

**در برابر اجرای دوباره امن است.** `subscription_notifications` روی انقضایی
که پیام درباره‌اش رفته کلید خورده، پس اجرای دوم در یک شب چیزی نمی‌فرستد و
تمدید یک مجموعه‌ی تازه باز می‌کند.

**ردیف قبل از پیامک نوشته می‌شود.** پیامک تکراری پول می‌گیرد و بی‌دقت به‌نظر
می‌رسد؛ پیامی که ثبت شده ولی نرفته یک هشدار هزینه دارد و بعدی چند روز بعد
است.

⚠️ **شکست یک کارگاه بقیه را متوقف نمی‌کند.** هر کدام wrap و شمرده می‌شود و
عدد در گزارش و در exit code برمی‌گردد.

### حذف داده — سه قدم، به همین ترتیب

```
۱. کلیدهای شیء را از ردیف‌ها بخوان
۲. اشیاء را پاک کن (کلیدهای خوانده‌شده + جاروی پیشوند)
۳. ردیف‌ها را پاک کن، به ترتیب وابستگی
```

⚠️ **هیچ‌کدام از دو روش شیء به‌تنهایی کافی نیست.** کارگاهی که از بکاپ با
شناسه‌ی تازه بازیابی شده، کلیدهایش پیشوند قدیمی دارند و جارو از کنارشان رد
می‌شود؛ و آپلودی که ردیفش هرگز ننشسته، ردیفی ندارد که کلیدش خوانده شود.

⚠️ **اگر ردیف‌ها اول بروند، کلیدها برای همیشه رفته‌اند** و عکس‌ها تا ابد در
باکت پول می‌گیرند — فضا تنها هزینه‌ای است که پایین نمی‌آید.

**ترتیب حذف دستی نوشته شده** چون هیچ cascade ای نیست: خطوط فاکتور قبل از
اقلام (`Restrict`)، فاکتور تعمیر قبل از دستگاه (`Restrict`).

⚠️ **نام delegateها در Prisma مفرد است** — `tx.user` نه `tx.users`. اشتباهش
خطای کامپایل می‌دهد نه شبی که cron نصفه کار می‌کند، و به همین دلیل فهرست
علیه `TransactionClient` تایپ شده.

### کد تخفیف در لحظه‌ی تأیید خرج می‌شود، نه در checkout

`payments.discount_code_id` می‌گوید این تلاش کدام کد را استفاده کرده؛ ردیف
`discount_code_uses` وقتی نوشته می‌شود که **پول واقعاً جابه‌جا شده**. کارتی که
سه بار رد شود نباید تنها استفاده‌ی مشتری را روی پولی که هرگز نرفت بسوزاند.

### پیش‌نمایش قیمت (۸.۸)

`POST /subscription/quote` **همان `quotePrice` و همان `usableDiscountCode`**
را صدا می‌زند که checkout — پس عددی که روی صفحه است و عددی که به زیبال می‌رود
نمی‌توانند اختلاف داشته باشند. پیاده‌سازی دوم برای پیش‌نمایش، دقیقاً راهی است
که اختلاف پیدا می‌کردند.

**`POST` برای چیزی که نمی‌نویسد**، چون `GET` یعنی کد تخفیف در لاگ سرور و در
تاریخچه‌ی مرورگر.

**کد رد شده `200` با یک فلگ است، نه `400`** — کاربر دارد تایپ می‌کند و رد
کردن هر ضربه‌ی کلید یعنی خطا زدن به کدی که هنوز کامل نشده.

### صفحه‌ی بازگشت یک مسیر فرانت است

ریدایرکت از دامنه‌ی دیگر کوکی `SameSite=Strict` ما را حمل نمی‌کند، پس
callback ای که سمت سرور مدیریت شود بدون session می‌رسد — و بستن آن شکاف یعنی
دریچه‌ی چهارم `SECURITY DEFINER`.

⚠️ **از query string هیچ‌چیز جز `trackId` گرفته نمی‌شود.** `success=1` چیزی
است که هر کسی می‌تواند در نوار آدرس تایپ کند.

### رسید، سربرگ کارگاه ندارد

`InvoicePreview` چیزی را چاپ می‌کند که کارگاه به مشتری می‌دهد؛ رسید اشتراک
چیزی است که **دوفیکسو به کارگاه** داده. لوگوی کارگاه رویش یعنی رسیدی که
به‌نظر می‌رسد خودش به خودش صورت‌حساب داده. الگوی چاپ همان است — دیالوگ خود
مرورگر، بدون وابستگی PDF.

### یکتایی

| ستون              | دامنه         | دلیل                                      |
| ----------------- | ------------- | ----------------------------------------- |
| `User.username`   | **سراسری**    | یک شماره = یک حساب در کل پلتفرم           |
| `Item.code`       | per-workspace | یک کارگاه نباید کد را برای بقیه رزرو کند  |
| `Category.name`   | per-workspace | همان                                      |
| `*.invoiceNumber` | per-workspace | شماره‌گذاری هر کارگاه مستقل               |
| نام کارگاه        | **یکتا نیست** | دو مغازه در دو شهر می‌توانند هم‌نام باشند |

### پول و تاریخ

- مبالغ `Decimal(18, 0)` · نرخ‌ها `Decimal(5, 2)` · در کنترلر `.toNumber()`
- ذخیره میلادی، API رشته‌ی ISO، نمایش جلالی در فرانت
- مرزهای «امروز» در گزارش‌ها **UTC** — ساعت ۳:۳۰ بامداد تهران صفر می‌شود

### گواهی TLS دستی است — انقضا ۲۵ نوامبر ۲۰۲۶

گواهی رایگان سه‌ماهه از پارس‌پک، احراز DNS TXT، صادرشده توسط Let's Encrypt.
روی سرور در `/home/reza/certs/` و read-only داخل Caddy mount می‌شود.

**چرا از فایل و نه ACME:** سرور بین‌الملل ندارد و Caddy به
`acme-v02.api.letsencrypt.org` نمی‌رسد. دستور `tls` صریح به Caddy می‌گوید این
گواهی را اصلاً مدیریت نکند — لاگ تأیید می‌کند که هیچ تلاشی برای صدور نمی‌کند.

**چرا DNS و نه HTTP:** روش HTTP نیاز دارد مرجع صدور به سرور ما برسد، که
بی‌بین‌الملل ممکن نیست. در روش DNS سرور اصلاً شرکت نمی‌کند — مرجع فقط رکورد
TXT را از Vercel می‌خواند.

⚠️ رکورد باید `_acme-challenge.app` باشد نه `_acme-challenge`، وگرنه برای
دامنه‌ی اشتباه اعتبارسنجی می‌شود.

⚠️ **CAA اجباری است.** `dofixo.ir` سه مرجع مجاز داشت و Certum بینشان نبود.
CAA روی `app` اضافه شد: `certum.pl` · `certum.com` · `letsencrypt.org` ·
`pki.goog` · `sectigo.com`. مرجع صدور اولین جایی که رکورد CAA پیدا کند متوقف
می‌شود، پس رکورد روی `app` جلوی بالا رفتن به apex را می‌گیرد — به همین دلیل
`letsencrypt.org` هم آنجا لازم بود.

⚠️ **هیچ‌چیز تمدیدش نمی‌کند.** یادآور تقویم: **۲۵ اکتبر ۲۰۲۶**.
مسیر خروج: اگر بین‌الملل فعال شد، دو خط `tls` از Caddyfile حذف شود و Caddy
هر ۶۰ روز خودش می‌گیرد.

⚠️ کلید خصوصی را پارس‌پک تولید کرده، پس نسخه‌ای دستشان است. گواهی بعدی با CSR
ساخته‌شده روی سرور.

### DNS

`app.dofixo.ir` → `45.156.186.60`، رکورد A، TTL 60. DNS دامنه روی Vercel است
(لندینگ Astro آنجاست). رکورد صریح `app` بر ALIAS wildcard اولویت دارد.

---

## ۵. تسک‌های باقی‌مانده

```markdown
- [~] 5.6 Fineti import — تا وقتی یک فایل نمونه نباشد، نوشتنش حدس زدن است
```

### فاز ۶ — تست

```markdown
- [x] 6.4 Integration tests against a real test Postgres database
- [ ] 6.1 Audit controller unit tests for gaps rather than starting over
- [ ] 6.2 Unit tests for business logic (invoiceTotals is done)
- [ ] 6.3 Auth tests — بیشترش در فاز ۳ نوشته شد؛ ممیزی لازم است
- [ ] (تازه) Frontend testing با Vitest — بعد از TypeScript شدن فرانت
```

### فاز ۷ — استقرار

```markdown
- [x] 7.0 پروفایل پردازش تصویر
- [x] 7.1 Dockerfile پروداکشن بک‌اند
- [x] 7.2 Dockerfile پروداکشن فرانت
- [x] 7.3 docker-compose.prod.yml
- [x] 7.3a سخت‌سازی اپ: JWT_SECRET بدون fallback · trust proxy عددی · CORS
- [x] 7.4 Caddy با TLS از فایل
- [x] 7.5 خرید VPS (پارس‌وی‌دی‌اس IR_VPS_05)
- [x] 7.6 اولین استقرار دستی
- [x] 7.6a roles در مهاجرت · کلید export بیرون از پیشوند کارگاه
- [ ] 7.7 GitHub Actions با `pnpm test:all`
- [x] 7.8 lifecycle rule روی `exports/` — ۳۰ روز، ۷ روز برای آپلود ناقص
```

### فاز ۸ — اشتراک و پرداخت

```markdown
- [x] 8.1 اسکیما و مهاجرت — هشت مدل، ۷ policy، سه پلن، backfill
- [x] 8.2 موتور اشتراک — extendSubscription · startTrial · کد رفرال
- [x] 8.3 نگهبان فقط-خواندنی با ۴۰۲ و مهلت ارفاق
- [x] 8.4 قیمت‌گذاری سمت سرور
- [x] 8.5 اتصال زیبال — checkout · verify · تاریخچه‌ی پرداخت
- [x] 8.6 رفرال — کد در ثبت‌نام، پاداش پس از تأیید پرداخت دعوت‌شده
- [x] 8.7 cron روی هاست — یادآوری، پیامک، فقط-خواندنی، حذف، تسویه
- [x] 8.8 فرانت — صفحه‌ی اشتراک، پیش‌نمایش قیمت، بنر، صفحه‌ی بازگشت
- [x] 8.9 فرانت — صفحه‌ی دعوت، فیلد کد در ثبت‌نام، رسید قابل چاپ
```

🔴 **فاز ۸ کامل است ولی مستقر نشده.** چک‌لیست استقرار در §۱۲.

⚠️ **۸.۷ تنها تسکی است که زیرساخت تازه می‌خواهد.** اپ هیچ زمان‌بندی‌ای ندارد
(در ۱.۴ عمداً `backupScheduler` حذف شد). تصمیم: cron روی هاست، هم‌شکل
`backup-database.sh`، نه `node-cron` داخل پروسه — چون اپ خرابی که پیامک
تمدید را هم با خودش ببرد، دقیقاً همان اشتباهی است که بکاپ از آن پرهیز کرد.

⚠️ **۸.۷ باید در برابر اجرای دوباره امن باشد.** جدول
`subscription_notifications` با کلید مرکب روی
`(workspace, kind, expiresAtSnapshot)` همین است: اجرای دوم در یک روز چیزی
نمی‌فرستد، ولی تمدید یک مجموعه‌ی تازه باز می‌کند.

⚠️ **حذف داده در ۸.۷ سه جا را لمس می‌کند:** ردیف‌های دیتابیس، عکس‌های آروان
زیر `workspaces/{id}/`، و `exports/{id}/`. فقط ردیف‌ها یعنی عکس‌هایی که تا
ابد هزینه می‌دهند.

### فاز ۹ — یکدست‌سازی رابط کاربری

```markdown
- [ ] 9.1 Fold the stock report into the items page as a filter
- [ ] 9.2 Move the profit summary onto the dashboard
- [ ] 9.3 Let the purchase invoice form create a complete item inline
- [ ] 9.4 Decide how deleting a purchase invoice should affect avg_purchase_price
- [ ] 9.5 A proper invoice template: editable layout, logo/stamp/signature placement
- [ ] 9.6 Remove `settings.invoice_prefix`, unused since 2.8
```

### فاز ۱۰ — شانزده باگ فرانت

هرکدام مستقل، شماره‌دار در `Roadmap.md`.

---

## ۶. بدهی‌های فنی و ریسک‌های شناخته‌شده

### ۱. فرانت هیچ تست خودکاری ندارد

مهم‌ترین شکاف پروژه. حالا TypeScript دارد که شکل داده را تضمین می‌کند، ولی
هیچ چیز رفتار را تضمین نمی‌کند. هر تأیید در این مهاجرت دستی بود.

### ۲. تک نقطه‌ی خرابی

یک سرور یعنی اگر آن ماشین بمیرد همه‌چیز می‌میرد. تنها چیزی که بین تو و فاجعه
می‌ایستد، `pg_dump` شبانه است. آگاهانه پذیرفته شده.

### ۳. کلید خصوصی `age` — ✅ دو نسخه موجود است

در password manager و یک جای فیزیکی دیگر. اگر روزی جابه‌جا شد، همین قاعده.

### ۴. باکت آروان جدا شد ✅ ولی کلید نه → بدهی ۲۷

### ۵. URL امضاشده ۱۵ دقیقه اعتبار دارد

اگر کاربر مودالی را بیست دقیقه باز بگذارد، عکس‌ها ۴۰۳ می‌گیرند. تصمیم: فعلاً کاری
نکنیم؛ اگر آزارنده شد یک `onError` با **یک بار** تلاش اضافه می‌شود.

### ۶. ثبت‌نام بدون تأیید هویت — ✅ حل شد با OTP.4

### ۷. سقف مطلق عمر session نداریم

هر چرخش ۳۰ روز تازه می‌دهد. تصمیم عمدی.

### ۸. بازیابی جزئی و ادغام پوشش داده نشده‌اند

کل کارگاه برمی‌گردد یا هیچ. اگر کارگاه بعد از حادثه داده‌ی تازه ساخته باشد، بازیابی
کارگاه دومی می‌سازد.

### ۹. اسکریپت‌های `ops/` تست خودکار ندارند

bash هستند و در Jest نمی‌گنجند. تنها تضمینشان تمرین دوره‌ای است.

### ۱۰. `settings.invoice_prefix` کد مرده است → **۹.۶**

### ۱۱. هر کوئری یک تراکنش با دو رفت‌وبرگشت است

`getDashboardStats` هفده کوئری موازی روی pool ده‌اتصالی. تنظیم pool تا وقتی عدد
واقعی نداریم عمداً انجام نشده.

⚠️ **این یک بار واقعاً شکست:** خروجی داده شش کوئری موازی می‌زد و درخواست عادی
کنارش «Unable to start a transaction» می‌گرفت. سریال شد. داشبورد همین مسئله
را دارد و هنوز موازی است.

### ۱۲. `DeprecationWarning` از `pg` — احتمالاً هم‌ریشه با ۱۱

در لاگ پروداکشن هم دیده می‌شود.

### ۱۳. raw SQL زیر extension نمی‌رود

هر raw query روی جدول tenant باید داخل `runInWorkspaceTransaction` باشد.

### ۱۴. تست‌های یکپارچگی در CI نیستند → **۷.۷** باید `pnpm test:all` بزند

⚠️ و تا آن روز، `jest.setup.ts` تنها چیزی است که متغیرهای محیطی را برای
تست‌ها می‌سازد. هر ماژولی که در زمان import پرتاب می‌کند باید آنجا خطی داشته
باشد، وگرنه روی ماشین تازه یا در CI کل سوئیت با خطایی می‌میرد که به جای
اشتباه اشاره می‌کند.

### ۱۵. `seed.ts` idempotent نیست

اگر کارگاهی وجود داشته باشد رد می‌شود. برای seed دوباره `docker compose down -v`.
از ۷.۶الف به بعد فقط درباره‌ی کارگاه پیش‌فرض است؛ `roles` در مهاجرت است و
مشکلی ندارد.

### ۱۶. حذف فاکتور خرید `avgPurchasePrice` را اصلاح نمی‌کند → **۹.۴**

### ۱۷. بهای تمام‌شده در گزارش سود، قیمت **فعلی** است، نه قیمت زمان فروش

### ۱۸. گزارش سود اقلام دلخواه را نادیده می‌گیرد

### ۱۹. `repair_invoice_items.itemId` عمداً رابطه نیست (پلی‌مورفیک)

### ۲۰. `personnel_id` روی `devices` بلااستفاده است. کاندید حذف

### ۲۱. سه ستون تخفیف در `SaleInvoiceItem` بلااستفاده‌اند

### ۲۲. `dist` خودش را پاک نمی‌کند · `pnpm-workspace.yaml` داخل `backend/` است

### ۲۳. شانزده مورد فرانت در فاز ۱۰

حین مهاجرت TypeScript پیدا شدند و عمداً دست‌نخورده ماندند: تبدیلی که رفتار
را هم عوض کند، تبدیلی است که کسی نمی‌تواند مرورش کند.

### ۲۴. باندل تک‌chunk ۷۲۲ کیلوبایت — **۱۷۲ کیلوبایت بعد از gzip** → ۱۰.۱۴

nginx پروداکشن `gzip on` دارد، پس آنچه کاربر واقعاً دانلود می‌کند ۱۷۲ کیلوبایت
است. هنوز ارزش code-splitting را دارد، ولی نه با فوریتی که قبلاً ثبت شده بود.

### ۲۵. کد تأیید پنج‌رقمی و بدون نمک هش شده است

هش تقریباً هیچ نمی‌دهد — صد هزار حالت، جدول رنگین‌کمانی در چند ثانیه.
امنیت واقعی از عمر سه‌دقیقه‌ای، سقف سه تلاش، و یک‌بارمصرف بودن می‌آید.
کامنت `code_hash` در اسکیما و مهاجرت همین را می‌گوید، چون کسی که فقط ستون
را ببیند فکر می‌کند جدول امن است و ممکن است سقف تلاش را بردارد.

### ۲۶. `prismaExtension.test.ts` به Postgres محلی وابسته است

سوئیت mock است ولی `jest.setup.ts` یک `DATABASE_URL_APP` واقعی می‌دهد و
تست عمداً روی سوکت شکست می‌خورد. روی ماشینی بدون Postgres شکل خطا فرق می‌کند
و تست ممکن است به دلیل دیگری پاس شود. → ۶.۱

### ۲۷. کلید آروان بین توسعه و پروداکشن مشترک است

آروان دسترسی per-bucket نمی‌دهد. باکت‌ها جدا شدند (`dofixo-prod` و
`reza-app-test-1`)، ولی یک کلید به هر دو می‌رسد — یعنی `.env` توسعه‌ی روی
لپ‌تاپ به باکت پروداکشن دسترسی دارد. پذیرفته‌شده تا وقتی آروان امکانش را بدهد.

### ۲۸. استقرار کاملاً دستی است

build روی لپ‌تاپ، `docker save`، `scp` (~۲۵ دقیقه برای ایمیج migrate)،
`docker load`. هیچ‌چیز خودکار نیست و هیچ rollback ای وجود ندارد جز نگه داشتن
ایمیج قبلی روی سرور. → ۷.۷

### ۲۹. سهمیه‌ی فضا به‌ازای کارگاه وجود ندارد

storage تنها منبعی است که هزینه‌اش per-tenant متغیر است. «ویژگی یکسان در همه‌ی
پلن‌ها» برای CPU درست است، برای این نه. **تنها بدهی‌ای که با گذشت زمان
گران‌تر می‌شود** — مشتری‌ای که سه سال پیش رفت هنوز هزینه تولید می‌کند.

### ۳۰. امضای presigned URL کش نمی‌شود

هر امضا یکتاست، پس مرورگر هیچ‌وقت کش نمی‌کند و هر بار باز شدن مودال همه‌ی
عکس‌ها را دوباره دانلود می‌کند. thumbnail شدت این را کم کرد ولی حل نکرد.

### ۳۱. 🔴 پنج منبع بدون بررسی نقش روی سرور — و نیمه‌ی فرانتش (AUTH.5)

⚠️ **نیمه‌ی تازه‌کشف‌شده:** `/dashboard` و `reports/*` در سایدبار
`adminOnly: true` اند ولی در `App.tsx` هیچ `ProtectedRoute minRole` ندارند —
تکنسین لینکشان را نمی‌بیند ولی با تایپ آدرس واردشان می‌شود. و `reports` یکی
از همان پنج روت بک‌اندی است که بررسی نقش ندارد، **پس هر دو لایه بازند.**

`items` · `purchaseInvoices` · `saleInvoices` · `repairInvoices` · `reports`
هیچ `atLeast("admin")` ندارند، در حالی که سایدبار همه را `adminOnly` علامت
زده. یعنی تکنسین با توکن خودش قیمت خرید، حاشیه‌ی سود و کل انبار کارفرمایش
را می‌خواند **و می‌نویسد**.

⚠️ **RLS اینجا کمکی نمی‌کند** — داده مال کارگاه خودش است. مسئله نقش است نه
مستأجر، و این تنها لایه‌ای است که فاز ۲ پوششش نمی‌دهد.

خطر امروز صفر است چون تکنسینی وجود ندارد. **قبل از اینکه اولین کارگاه واقعی
پرسنل بسازد باید بسته شود.** → بخش AUTH در روادمپ

**چطور پیدا شد:** ۱۰.۸ به‌عنوان باگ مسیریابی فرانت ثبت شده بود. موقع بررسی
معلوم شد بک‌اند هم باز است، و بعد که همه‌ی روت‌فایل‌ها با یک حلقه چک شدند،
پنج تا بودند نه یکی.

```bash
for f in src/routes/*.ts; do
  printf '%-28s ' "$(basename $f)"
  grep -o 'atLeast([^)]*)\|authorize([^)]*)' "$f" | sort -u | tr '\n' ' '
  echo
done
```

### ۳۲. یک کوئری اضافه روی هر درخواست نوشتن

نگهبان ۸.۳ انقضا را از دیتابیس می‌خواند، و با RLS هر کوئری دو رفت‌وبرگشت
است. گذاشتنش در JWT بررسی و رد شد: توکن ۱۵ دقیقه عمر دارد، پس کارگاهی که
همین الان پرداخت کرده تا ربع ساعت قفل می‌ماند و کسی که دستی منقضی شده تا
ربع ساعت می‌نویسد.

پذیرفته‌شده: نوشتن‌ها اقلیت درخواست‌هایند و خواندن‌ها اصلاً کوئری نمی‌زنند.

### ۳۳. شمارش استفاده‌ی کد تخفیف از `COUNT(*)` می‌آید، نه از یک ستون

عمدی: ستون شمارنده روی داده‌ی مرجع، مجوز نوشتن برای نقش اپ می‌خواست، و
شمارنده‌ای که با ردیف‌هایش ناهماهنگ شود از نبودنش بدتر است. بهایش یک کوئری
اضافه در هر checkout با کد تخفیف است.

### ۳۴. رسید در حالت کف مبلغ جمع نمی‌زند

اگر کد تخفیف از قیمت پلن بزرگ‌تر باشد، `قیمت − تخفیف` با مبلغ نهایی
نمی‌خواند. عمدی: آن حالت یعنی کد اشتباه ساخته شده، و عدد ناجور دیدنی‌ترش
می‌کند از رسیدی که قشنگ جمع می‌زند و اشتباه را پنهان می‌کند.

### ۳۶. `prisma db seed` پیکربندی نشده

`prisma.config.ts` بخش `migrations.seed` ندارد، پس `migrate reset` بعد از
پاک کردن دیتابیس، seed را اجرا نمی‌کند. یک بار باعث شد کاربر ناپدید شود و
ورود `401` بدهد بدون اینکه معلوم باشد چرا. راه دستی: `pnpm exec tsx prisma/seed.ts`.

### ۳۷. `quoteFor` و `checkout` همان کوئری‌ها را جدا می‌زنند

هر دو پلن و رفرال و شمار پرداخت را می‌خوانند. عمدی ماند چون `checkout` به
`discountRow.id` نیاز دارد و `quoteFor` فقط `codeAccepted` برمی‌گرداند. اگر
روزی واگرا شوند، همان‌جا قیمت روی صفحه با قیمت درگاه اختلاف پیدا می‌کند.

### ۳۸. `/settings` در سایدبار و در روت هم‌نظر نیستند

سایدبار `adminOnly: false` دارد ولی روت داخل `ProtectedRoute minRole="admin"`
است — تکنسین لینک را می‌بیند و با کلیک رد می‌شود.

### ۳۹. ✅ رسیدها در sandbox بدون شماره پیگیری‌اند — روی پروداکشن سالم

حساب آزمایشی زیبال `refNumber` و `cardNumber` را خالی برمی‌گرداند. در
استقرار ۲ سپتامبر با یک پرداخت واقعی بررسی شد: هر دو پر آمدند
(`357340889094` و `621986******1853`). فقط مسئله‌ی sandbox بود.

### ۴۰. 🔴 پیامک تأیید پرداخت نوشته نشده — تسک ۸.۱۰

`settlePayment` اشتراک را تمدید می‌کند، کد تخفیف را خرج می‌کند و
`rewardReferrer` را صدا می‌زند — ولی هیچ‌جا `sendTemplate` با
`PAYMENT_OK` صدا زده نمی‌شود. در `subscriptionController.ts` این رشته
اصلاً وجود ندارد.

**چرا هیچ دروازه‌ای نگرفتش:** `SMS_TEMPLATES.PAYMENT_OK` در `lib/sms.ts`
اعلام شده، `SMS_TEMPLATE_PAYMENT_OK` در هر پنج فایل محیط هست،
`requireTemplateId` موقع بوت اعتبارسنجی‌اش می‌کند، قالب `344895` در پنل
sms.ir تأیید شده، و همین سند آن را در جدول قالب‌ها با «کِی: پس از تأیید»
ثبت کرده بود. **همه‌ی نشانه‌ها می‌گفتند کار شده است.**

🔴 **چیزی که وجود ندارد، تستی هم که شکست بخورد ندارد.** ۵۰۶ تست واحد و شش
سوئیت یکپارچگی همه سبز بودند. تنها چیزی که این را می‌گرفت یک تست بود که
ادعا کند «پس از `settlePayment` موفق، `sendTemplate` با `PAYMENT_OK` صدا
زده شده».

⚠️ **پیامک پاداش رفرال هم نیامد.** `rewardReferrer` خطاهایش را می‌بلعد؛
اگر بدون لاگ می‌بلعد، آن نقض `RULES.md §۶` است و همان چیزی که هر دو را
نامرئی کرد. در ۸.۱۰ بررسی شود.

⚠️ **یادآوری‌های انقضا سالم‌اند.** `notify()` در `subscriptionJob.ts`
نوشته شده و لاگ هم دارد — این محدود به مسیر پرداخت است.

### ۴۱. timeout زیبال روی شبکه‌ی سرور کوتاه است

در استقرار ۲ سپتامبر یک `requestPayment` با
`Could not reach Zibal: The operation was aborted due to timeout` رد شد و
ردیف پرداخت روی `pending` ماند. تلاش دوم موفق بود.

`lib/sms.ts` قبلاً به همین دلیل از ۱۰ ثانیه به ۲۰ رفت (شرایط شبکه‌ی
ایران). `lib/zibal.ts` احتمالاً هنوز مقدار کمتری دارد.

⚠️ برای مشتری این یعنی کلیک روی «پرداخت» و هیچ اتفاقی نیفتادن — با یک
پیام عمومی که نمی‌گوید دوباره تلاش کن. `settleAbandonedPayments` این
ردیف‌ها را شبانه جمع می‌کند، ولی آن برای پولی است که جابه‌جا شده، نه
درخواستی که اصلاً نرسیده.

### ۳۵. سهمیه‌ی فضا هنوز نیست، و حالا پلن هم دارد

بدهی ۲۹ همچنان باز است، ولی از فاز ۸ به بعد شکل تازه‌ای گرفته: سه پلن
فروخته می‌شود که همه‌شان «امکانات یکسان» دارند، و اگر روزی سهمیه اضافه شود
باید با مشتری‌های موجود هماهنگ شود نه یک‌طرفه. **نوشتنش در شرایط استفاده
الان مجانی است.**

---

## ۷. قواعد کاری (`RULES.md`)

| #   | قاعده                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ۱   | **یک تسک شماره‌دار در هر نشست**                                                                                                                                                        |
| ۲   | **هرگز بدون تأیید صریح کامیت نکن**                                                                                                                                                     |
| ۳   | هر تسکی که منطق backend را لمس می‌کند تست لازم دارد · **قبل از اعلام تمام شدن: `pnpm lint && pnpm build && pnpm test`، هر سه** · منبع REST جدید = یک خط در `isolation.test.ts`         |
| ۴   | Conventional Commits، کامیت‌های کوچک                                                                                                                                                   |
| ۵   | روی `feature/multi-tenant-migration` بمان                                                                                                                                              |
| ۶   | الگوی موجود · بدون کد مرده · Zod روی همه‌ی ورودی‌ها · **هرگز `workspaceId` از کلاینت نگیر**                                                                                            |
| ۷   | امنیت: `workspaceId` + RLS · raw SQL داخل helper · جدول جدید = policy در همان مهاجرت · مسیر آپلود = `restoreWorkspaceContext` بعد از multer · دریچه‌ی چهارم بدون توضیح در COMMENT نساز |
| ۸   | بعد از تکمیل تسک `Roadmap.md` را به‌روز کن                                                                                                                                             |
| ۹   | ارائه: چه شد → نتیجه‌ی تست → ریسک‌ها → منتظر تأیید                                                                                                                                     |
| ۱۰  | **دستورات شبکه‌ای را خودت اجرا نکن**                                                                                                                                                   |

---

## ۸. دستورات روزمره

### راه‌اندازی از صفر (توسعه)

```bash
cp .env.example .env
cp backend/.env.example backend/.env
cp ops/backup.env.example ops/backup.env

docker compose up -d --build

node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
docker compose exec postgres psql -U dofixo -d dofixo_dev \
  -c "ALTER ROLE dofixo_app WITH LOGIN PASSWORD '<رمز>';"

cd backend && pnpm exec prisma migrate dev && pnpm exec prisma generate
cd backend && pnpm seed        # با SEED_ADMIN_USERNAME وارد می‌شوید
```

### دیتابیس تست (یک بار)

```bash
docker compose exec postgres psql -U dofixo -d postgres \
  -c "CREATE DATABASE dofixo_test OWNER dofixo;"
```

### چرخه‌ی توسعه

```bash
cd backend && pnpm test              # ۵۰۶ تست با mock
cd backend && pnpm test:integration  # ۱۱۵ تست با دیتابیس واقعی
cd backend && pnpm test:all
cd backend && pnpm lint && pnpm build
cd frontend && pnpm lint && pnpm build

docker compose logs backend --tail 30
docker compose restart backend
```

### استقرار پروداکشن

**روی لپ‌تاپ** — ایمیج‌ها اینجا ساخته می‌شوند، هرگز روی سرور:

```bash
cd /Programming/Code/Dofixo
docker compose -f docker-compose.prod.yml --env-file .env.prod build backend frontend
docker save dofixo-backend:prod dofixo-frontend:prod | gzip > /tmp/img.tar.gz
scp /tmp/img.tar.gz dofixo:/tmp/
```

**روی سرور:**

```bash
cd ~/dofixo
gunzip -c /tmp/img.tar.gz | docker load && rm /tmp/img.tar.gz

# فقط وقتی مهاجرت تازه‌ای هست (ایمیج migrate هم باید منتقل شده باشد)
docker compose -f docker-compose.prod.yml --env-file .env.prod \
  --profile tooling run --rm migrate

docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
docker compose -f docker-compose.prod.yml --env-file .env.prod ps
docker compose -f docker-compose.prod.yml --env-file .env.prod logs backend --tail 30
```

⚠️ **`--force-recreate backend` بعد از هر تغییر `.env.prod`** — `restart` کافی
نیست.

🔴 **هرگز `down -v` روی سرور.** volume نام‌دار است و `-v` دیتابیس را پاک
می‌کند. در توسعه این دستور روال عادی seed دوباره است (بدهی ۱۵)، پس عادتی است
که ناخواسته منتقل می‌شود.

### عملیات

```bash
./ops/backup-database.sh                                    # بکاپ دستی
./ops/extract-workspace.sh <backup.age> <id> <key-file>     # استخراج یک کارگاه
./ops/reset-password.sh <شماره>                             # بازیابی دسترسی

cat ~/dofixo-backup.log                                     # روی سرور
```

### متغیرهای محیطی — `backend/.env` (توسعه)

```dotenv
DATABASE_URL=postgresql://dofixo:dofixo@127.0.0.1:5432/dofixo_dev?schema=public
DATABASE_URL_APP=postgresql://dofixo_app:<رمز>@127.0.0.1:5432/dofixo_dev?schema=public
TEST_DATABASE_URL=postgresql://dofixo:dofixo@127.0.0.1:5432/dofixo_test?schema=public
TEST_DATABASE_URL_APP=postgresql://dofixo_app:<رمز>@127.0.0.1:5432/dofixo_test?schema=public

PORT=5001
JWT_SECRET=<crypto.randomBytes(32).toString('hex')>
TRUST_PROXY=0
SEED_ADMIN_USERNAME=09120000000   # باید موبایل معتبر باشد
SEED_ADMIN_PASSWORD=<رمز اولیه>
RATE_LIMIT_API=1000
RATE_LIMIT_LOGIN=10
RATE_LIMIT_OTP=3

S3_ENDPOINT=https://s3.ir-thr-at1.arvanstorage.ir
S3_REGION=default
S3_BUCKET=reza-app-test-1
S3_ACCESS_KEY=<کلید>
S3_SECRET_KEY=<کلید>

SMS_API_KEY=<کلید>
SMS_TEMPLATE_ID=<شناسه>

ZIBAL_MERCHANT=zibal          # ⚠️ کوچک — زیبال حساس است، ZIBAL کد ۱۰۴ می‌دهد
APP_URL=http://localhost:5173 # callback از آن ساخته می‌شود

SMS_TEMPLATE_BEFORE_EXPIRY=398956
SMS_TEMPLATE_ON_EXPIRY=764207
SMS_TEMPLATE_AFTER_EXPIRY=450597
SMS_TEMPLATE_PAYMENT_OK=344895
SMS_TEMPLATE_REFERRAL_REWARD=549585
```

🔴 **هر متغیر تازه باید در پنج جا اضافه شود، نه یکی:**

| جا                                  | برای چه           |
| ----------------------------------- | ----------------- |
| `backend/.env`                      | اجرای واقعی توسعه |
| `backend/.env.example`              | مستندسازی         |
| `backend/jest.setup.ts`             | تست واحد          |
| `backend/jest.integration.setup.ts` | تست یکپارچگی      |
| `.env.prod` روی سرور                | پروداکشن          |

⚠️ در ۸.۷ دقیقاً بدترین ترتیب اتفاق افتاد: فایل‌های تست به‌روز شدند و `.env`
نه — پس **تست‌ها سبز بودند و اپ بالا نمی‌آمد**. و چون `seed.ts` هم زنجیره‌وار
به `lib/sms.ts` می‌رسد، `migrate reset` بی‌صدا کاربر نساخت و ورود به اپ
`401` داد.

`.env` ریشه: `POSTGRES_APP_PASSWORD=<همان رمز>`
`ops/backup.env`: پیکربندی بکاپ، **مستقل از `backend/.env` عمداً**

### متغیرهای لازم برای بوت کانتینر پروداکشن

```
DATABASE_URL_APP · JWT_SECRET · SMS_API_KEY · SMS_TEMPLATE_ID
S3_ENDPOINT · S3_BUCKET · S3_ACCESS_KEY · S3_SECRET_KEY
ZIBAL_MERCHANT · APP_URL
SMS_TEMPLATE_BEFORE_EXPIRY · SMS_TEMPLATE_ON_EXPIRY
SMS_TEMPLATE_AFTER_EXPIRY · SMS_TEMPLATE_PAYMENT_OK
SMS_TEMPLATE_REFERRAL_REWARD
```

⚠️ **`ZIBAL_MERCHANT` در پروداکشن نباید `ZIBAL` باشد** — اپ عمداً بالا
نمی‌آید. و **`APP_URL` باید `https://app.dofixo.ir` باشد**، وگرنه callback به
لپ‌تاپ اشاره می‌کند و زیبال با کد ۱۰۶ ردش می‌کند.

⚠️ این ماژول‌ها **در زمان import** پرتاب می‌کنند، نه در اولین استفاده. یک متغیر
جامانده یعنی کانتینری که اصلاً بالا نمی‌آید و در حلقه‌ی ری‌استارت می‌افتد — نه
اپی که ساکت نصفه کار کند. همان الگوی «fallback نداریم» در `lib/prisma.ts`.

⚠️ `TRUST_PROXY=1` در پروداکشن **الزامی** است. با `0`، کل پلتفرم سه پیامک در
ساعت می‌گیرد چون همه‌ی درخواست‌ها IP کانتینر Caddy را دارند.

اندازه‌ی ایمیج‌ها: backend ~۶۸۰ مگابایت · frontend ~۴۹ · migrate ~۱٫۳ گیگ.

---

## ۹. دام‌هایی که در مسیر به آن‌ها خوردیم

### multer از async context عبور نمی‌کند

پرهزینه‌ترین دام فاز ۴. علامتش: `req.user` هست ولی `currentWorkspaceId()` برابر
`undefined`. راه‌حل: `restoreWorkspaceContext` بعد از multer، که store **تازه** باز
کند.

### `sharp` بدون `.rotate()` عکس را کج ذخیره می‌کند

نه تگ EXIF Orientation را اعمال می‌کند، نه منتقلش می‌کند. یازده از پانزده
عکس واقعی تعمیرگاه جهت غیرپیش‌فرض داشتند (۶، ۸ یا ۳). علامتش عکسی است که
در گالری گوشی درست و در اپ چرخیده است — که شبیه باگ CSS به نظر می‌رسد.

`.rotate()` بدون آرگومان جهت را اعمال و تگ را حذف می‌کند، که مختصات GPS
دوربین را هم پاک می‌کند.

### کاراکتر `<` در کپی از چت گم می‌شود

بعد از هر کپی:

```bash
cd backend && grep -n "z.infer\|GetPayload\|queryRaw\|satisfies" src/**/*.ts
```

مطمئن‌تر: فایل را با `cat > path << 'EOF'` بسازید.

### موقع اعمال «از → به»، خط قدیمی را حذف کنید

باگ دو-پاسخی `refresh` از همین آمد: خط تازه اضافه شد ولی قدیمی ماند، و هر رفرش
صفحه دو session ساخت.

### «از → به» برای فایل‌های بلند امن نیست

وقتی ویرایش‌ها بیش از سه‌چهار جای فایل را لمس می‌کنند، جا ماندن خط قدیمی
تقریباً حتمی است — دو بار پیش آمد (`DeviceDetailModal` و `Settings`). برای این
فایل‌ها، کل فایل با `cat > path << 'EOF'` تک‌قدم و بی‌خطر است.

### `pg_get_serial_sequence` روی جدول‌های سیستمی می‌شکند

منطق `setval` سه نسخه خواست. `information_schema.tables` جدول‌های سیستمی را
برمی‌گرداند و `pg_get_serial_sequence` روی جدولی که نیست **خطا پرتاب می‌کند** نه
`NULL` — و Postgres ترتیب ارزیابی `WHERE` را تضمین نمی‌کند. جواب `pg_sequences` بود.

### `sudo` نزنید

`prisma generate` فایل می‌نویسد و با `sudo` مالکشان root می‌شود. کانتینر هم موقع
بوت همین کار را می‌کند:

```bash
sudo chown -R "$USER":"$USER" backend
```

### بعد از `pnpm add` کانتینر را بازبسازید

علامتش `MODULE_NOT_FOUND` روی پکیج تازه‌نصب است:

```bash
docker compose up -d --build --renew-anon-volumes backend
```

### موقع کار روی migration کانتینر را بخوابانید

`tsx watch` می‌تواند migration را قبل از شما اجرا کند: `docker compose stop backend`.

### تست رمز از داخل کانتینر بی‌معنی است

`pg_hba.conf` برای `127.0.0.1` روی `trust` است. باید از **نام سرویس** رفت:

```bash
docker compose exec postgres env PGPASSWORD='wrong' \
  psql -h postgres -U dofixo_app -d dofixo_dev -c "SELECT 1;"
```

### `ERR_HTTP_HEADERS_SENT` همیشه باگ نیست

کلاینتی که وسط درخواست می‌رود پاسخ را می‌بندد. نگهبان `if (!res.headersSent)` قبل
از هر نوشتن، و در `catch` هم.

### در zsh دستور `.` مسیر نسبی را در `$PATH` می‌گردد

`. backup.env` شکست می‌خورد؛ `. ./backup.env` کار می‌کند.

### مهاجرت دستی: timestamp باید بزرگ‌تر باشد

### `globalSetup` فقط وقتی تستی باشد اجرا می‌شود

### mock کردن `lib/prisma` سه export دارد

`default`، `runInWorkspaceTransaction` و در صورت نیاز
`runInNewWorkspaceTransaction` — و helperها باید callback را **واقعاً** صدا بزنند:

```typescript
runInWorkspaceTransaction: jest.fn(
  (_workspaceId: number, fn: (tx: unknown) => unknown) => fn(tx),
),
```

### تستی که context لازم دارد باید در `runWithRequestContext` بپیچد

### `127.0.0.1` نه `localhost`

روی Arch، `localhost` اول به `::1` حل می‌شود و Prisma با `P1001` شکست می‌خورد.

### `prisma generate` بعد از هر `migrate dev` · Prisma 7 به driver adapter نیاز دارد

### `tsx watch` بعد از حذف فایل گیر می‌کند

اول `.ts` را بسازید، بعد `.js` را حذف کنید، بعد `docker compose restart backend`.

### `as const` با تایپ‌های Prisma کار نمی‌کند

### `z.coerce.date()` با رشته‌ی خالی

```typescript
z.preprocess((value) => (value === "" ? undefined : value), z.coerce.date().optional());
```

### ارقام فارسی و عربی‌هندی دو چیز متفاوتند

`۰۹۱۲` (U+06F0) و `٠٩١٢` (U+0660). `persianToEnglish` فقط اولی را می‌شناسد؛
`schemas/auth.ts` هر دو را.

### interfaceهای هم‌نام در TypeScript ادغام می‌شوند، خطا نمی‌دهند

برخلاف `type`. یک تعریف تکراری بی‌سروصدا با قبلی جمع می‌شود و تایپی می‌سازد
که هیچ پاسخ واقعی با آن جور نیست. در `types/api.ts` یک بار پیش آمد: نسخه‌ی
قدیمی `Customer` حذف نشد و `updated_at` ای که ستون ندارد باقی ماند.

### `git mv` یعنی `docker compose restart frontend`

Vite مسیر قدیمی را در گراف ماژول نگه می‌دارد و ۴۰۴ می‌دهد که شبیه import
شکسته به‌نظر می‌رسد، نه شبیه فایل جابه‌جاشده.

### narrowing روی `obj.prop` وارد closure نمی‌شود

`if (tx.reference_id !== null)` بعد قابل استفاده در `onClick` نیست: TypeScript
فرض می‌کند تا اجرای closure ممکن است عوض شده باشد. راه‌حل، گرفتنش در یک
`const` محلی قبل از JSX است.

### انتساب `undefined` به `process.env` رشته‌ی `"undefined"` می‌سازد

الگوی رایج `afterEach(() => { process.env.X = original })` وقتی متغیر از اول
تعریف نشده باشد، به‌جای پاک کردنش رشته‌ی `"undefined"` می‌گذارد — و آن را برای
هر چیزی که بعد اجرا شود جا می‌گذارد. در `productionHardening.test.ts` بلوک
CORS با `TRUST_PROXY="undefined"` شروع می‌شد که تست قبلی رهایش کرده بود.

```typescript
if (original === undefined) delete process.env.X;
else process.env.X = original;
```

### `EAI_AGAIN` روی مسیر Arvan یعنی DNS، نه کلید

اپراتور موبایل دامنه‌های آروان را جور دیگری جواب می‌دهد. آنچه کاربر می‌بیند
«هیچ عکسی پردازش نشد» است، که به‌نظر می‌رسد عکس مشکل داشته نه شبکه.

### ⚠️ whitelist در پنل sms.ir به شکل `HTTP 401` ظاهر می‌شود

نه `status: 10` در بدنه، نه پیامی درباره‌ی IP — فقط ۴۰۱، که در لاگ عیناً
شبیه «کلید نامعتبر» است. یک ساعت صرف بررسی کلید شد در حالی که کلید سالم بود.

**IP خانگی پویاست و VPN آدرس خروجی را عوض می‌کند**، پس این دوباره پیش
می‌آید. `curl -s https://api.ipify.org` آدرس فعلی را می‌دهد.

✅ IP سرور ثبت شد: `45.156.186.60`.

⚠️ **`HTTP 400` چیز دیگری است:** درخواست رسید و احراز هویت شد، ولی محتوا رد
شد — معمولاً `SMS_TEMPLATE_ID` اشتباه (قالب sandbox به‌جای پروداکشن).

### timeout به معنی نرفتن پیامک نیست

ده ثانیه روی اینترنت ایران کوتاه بود و به بیست رسید. ولی مسئله باقی است:
اگر sms.ir درخواست را گرفته و فرستاده باشد و فقط پاسخش دیر برسد، ما ردیف را
حذف می‌کنیم — و کاربر کدی می‌گیرد که در دیتابیس نیست.

نگه داشتن ردیف بدتر بود: هر قطعی سهمیه‌ی کاربر را می‌خورد. این حالت نادر
است و راه‌حلش «کد تازه بگیر» است.

### `has no installation candidate` روی سرور تازه یعنی `apt update` نزده‌ای

پارس‌وی‌دی‌اس میرور خودش را دارد (`ubuntu.parsvds.com`)، پس نبودِ اینترنت
بین‌الملل روی apt اثری ندارد. پیام شبیه «مخزن در دسترس نیست» به نظر می‌رسد
ولی معنی‌اش «فهرست بسته‌ها از زمان ساخت ایمیج کهنه است» است.

### `apt install a b` اگر `b` نباشد، `a` را هم نصب نمی‌کند

`sudo apt install -y age awscli` هیچ‌کدام را نصب نکرد. علامتش این است که فقط
خطای بسته‌ی دوم را می‌بینی و فرض می‌کنی اولی نصب شده — بعداً اسکریپت می‌گوید
`age is not installed` و به‌نظر می‌رسد مشکل جای دیگری است.

### SSH بی‌صدا هنگ می‌کند: تقصیر agent است، نه شبکه

علامت: `ssh` هیچ پیامی نمی‌دهد و هیچ‌وقت timeout نمی‌شود. در `ssh -vvv` دقیقاً
اینجا می‌ایستد:

```
debug1: Server accepts key: ...        ← کلید قبول شد
debug3: sign_and_send_pubkey: signing  ← اینجا برای همیشه
```

علتش `SSH_AUTH_SOCK=/run/user/1000/gcr/ssh` است — agent مربوط به GNOME
Keyring که برای امضا منتظر یک پنجره‌ی گرافیکی می‌ماند که باز نمی‌شود.
`ssh-copy-id` قبلش کار می‌کند چون با **رمز** احراز هویت می‌کند و هرگز به
مسیر امضا نمی‌رسد.

راه‌حل، در `~/.ssh/config` روی لپ‌تاپ: `IdentityAgent none` + `IdentitiesOnly yes`.

⚠️ `ssh-copy-id` **همه‌ی** کلیدهای agent را نصب می‌کند. یک کلید متعلق به شخص
دیگری روی سرور نشست و باید دستی پاک می‌شد. بعد از هر `ssh-copy-id`،
`cat ~/.ssh/authorized_keys` را بخوان.

### VPN تقصیرکار پیش‌فرض نیست

اولین حدس برای هنگ کردن SSH، VPN بود (طبق §۱۰ RULES). لاگ نشان داد VPN تمام
مدت روشن و سالم بود و مشکل جای دیگری است. `auth.log` قبل از حدس زدن.

### در build پروداکشن، `prisma generate` باید قبل از `tsc` باشد

generator از نوع `prisma-client` است و **TypeScript** تولید می‌کند، نه جاوااسکریپت
آماده. `tsconfig.json` با `include: ["src/**/*"]` آن را هم کامپایل می‌کند، پس
`src/generated/prisma` باید قبل از اجرای tsc وجود داشته باشد.

ترتیب اشتباه، خطای «module not found» روی هر کنترلر می‌دهد که به‌نظر می‌رسد
مشکل از import ها است.

⚠️ tsc فقط `.ts` و `.js` را منتقل می‌کند. اگر generator روزی فایل غیر-TS بدهد
(wasm، کپی schema)، بی‌صدا از dist غایب می‌شود — Dockerfile یک `find` دارد
که همین را جبران می‌کند.

### `NODE_ENV=production` در Dockerfile است، نه compose

اگر متغیر محیطی باشد، یک `.env` ناقص کوکی refresh را بدون `Secure` می‌فرستد و
اپ کاملاً سالم به‌نظر می‌رسد. همان استدلال fallback نداشتن `lib/prisma.ts`.

### 🔴 `ZIBAL_MERCHANT` به بزرگی و کوچکی حروف حساس است

`zibal` کار می‌کند؛ `ZIBAL` کد نتیجه‌ی **۱۰۴ — invalid merchant** می‌دهد، که
عیناً شبیه «حساب خراب است» می‌خواند نه «یک حرف بزرگ». پشتیبانی زیبال آن را با
حروف بزرگ نوشت و مستنداتشان کوچک — و کشف دوباره‌اش یک شب گرفت.

گارد پروداکشن عمداً `toLowerCase()` می‌کند تا هر دو املا را بگیرد.

### دو فایل setup برای Jest هست، نه یکی — و یک globalSetup سوم

`jest.setup.ts` و `jest.integration.setup.ts` هر دو باید هر متغیری که در
زمان import پرتاب می‌شود را داشته باشند. `jest.integration.globalSetup.ts` هم
`TEST_DATABASE_URL` را بدون پیش‌فرض می‌خواهد، که درست است — آدرس دیتابیس تست
باید از بیرون بیاید.

⚠️ سوئیت یکپارچگی مدت‌ها فقط چون `dotenv` متغیرها را از `backend/.env`
می‌خواند سبز بود — همان چیزی که در CI (۷.۷) خواهد شکست.

### `Date.now()` در بدنه‌ی رندر ممنوع است

قاعده‌ی `react-hooks/purity` صریحاً ردش می‌کند، و درست هم می‌گوید: شمارش
معکوسی که با هر رندر عوض شود، به زمانی که React اتفاقی دوباره اجرا شده وابسته
است. `const [now] = useState(() => Date.now())` — و **بالای همه‌ی returnهای
زودهنگام**، چون hook را نمی‌شود شرطی صدا زد.

### `prisma generate` بعد از هر مهاجرت لازم است

Prisma 7 خودش نمی‌زند. علامتش خطای `Property does not exist on type` روی
ستونی است که همین الان اضافه شده.

### `mkdir -p backend/...` از داخل `backend/` پوشه‌ی تودرتو می‌سازد

علامتش این است که Prisma می‌گوید «Already in sync» و تعداد مهاجرت‌ها یکی کم
است. فایل ساخته شده، فقط در `backend/backend/prisma/migrations/`. قبل از
هر دستور مسیرنسبی، به prompt نگاه کن.

### `migrate reset` قبل از `--create-only` است، نه بعدش

اگر مهاجرتی اعمال شده و بعد پوشه‌اش را حذف کنی، Prisma drift می‌گیرد و
مهاجرت تازه نمی‌سازد تا دیتابیس ریست شود. ترتیب درست: ریست، بعد
`--create-only`، بعد چسباندن بلوک دست‌نویس، بعد `migrate dev`.

⚠️ و بلوک SQL دست‌نویس را قبل از حذف پوشه جایی کپی بگیر.

### `docker compose exec` برای خواندن از stdin به `-T` نیاز دارد

بدون آن: `cannot attach stdin to a TTY-enabled container`.

### `COUNT(*)` روی جدول tenant، فقط ردیف‌های خودت را می‌شمارد

بدیهی وقتی نوشته می‌شود، نامرئی وقتی خوانده می‌شود. سقف `maxUses` کد تخفیف
از همین‌جا بی‌اثر بود: هر کارگاه صفر می‌دید. **هر جا عددی «سراسری» لازم
است، اول از خودت بپرس این کوئری زیر کدام policy می‌رود.**

### ⚠️ داکر قواعد ufw را دور می‌زند

هر `ports:` در compose مستقیم روی iptables می‌نشیند و ufw جلویش را نمی‌گیرد.
اگر اشتباهاً `"5432:5432"` نوشته شود، Postgres روی اینترنت باز است در حالی که
`ufw status` می‌گوید بسته. **علامتش هیچ خطایی نیست.**

راه‌حل انتخاب‌شده رادیکال‌تر است: **فقط Caddy پورت منتشر می‌کند.** چیزی که
publish نشود اصلاً نمی‌تواند اشتباه bind شود. تأیید شد — ۵۴۳۲ و ۵۰۰۱ از
اینترنت timeout می‌دهند.

### هر سرویس compose پروداکشن باید تگ `image:` صریح داشته باشد

هر دو فایل compose نام یکسان می‌سازند (`name: dofixo` و همان سرویس‌ها)، پس
build پروداکشن **ایمیج توسعه را بازنویسی می‌کند**. آنچه روی سرور می‌رود
`tsx watch` با root و بدون `NODE_ENV` است و هیچ خطایی نمی‌دهد.

⚠️ `migrate` هم لازمش دارد: بدون تگ، compose روی سرور دنبال `./backend`
می‌گردد و `unable to prepare context` می‌دهد.

### متغیر محیطی جدید = `--force-recreate`، نه `restart`

مقادیر هنگام **ساخت** کانتینر ثابت می‌شوند. علامتش «عوضش کردم ولی هنوز همان
خطا را می‌دهد» است.

### `>>` روی فایلی که خط جدید ندارد، به خط آخر می‌چسبد

`echo "X=y" >> .env` وقتی فایل با newline تمام نشده باشد، `RATE_LIMIT_OTP=3X=y`
می‌سازد. compose آن را «متغیر ست نشده» می‌خواند و خطایش به سرویس **دیگری**
اشاره می‌کند. `printf '\n...'` امن است.

⚠️ و `sed -i '/X/d'` بعدش **کل خط** را می‌برد، یعنی متغیر سالم قبلی هم می‌رود.

### الگوی `.env` در gitignore فایل‌های `.env.prod` را نمی‌گیرد

`.env` فقط فایلی دقیقاً به همان نام را می‌گیرد. `.env.prod` باید جدا نوشته شود —
و آن فایل رمز دیتابیس، `JWT_SECRET` و کلیدهای آروان و sms.ir را دارد.

### آنچه در روادمپ نوشته شده ممکن است نصف مسئله باشد

۱۰.۸ «باگ مسیریابی فرانت» ثبت شده بود چون از سمت فرانت پیدا شده بود. لایه‌ی
سرور اصلاً نگاه نشده بود. یک `grep` روی همه‌ی روت‌فایل‌ها پنج شکاف نشان داد
نه یکی.

**قبل از شروع هر تسک امنیتی، همان بررسی را روی کل خانواده‌اش بزن** — نه فقط
جایی که علامت‌گذاری شده.

### پیام «N backups before this run» یک بیشتر می‌شمارد

listing بعد از آپلود انجام می‌شود، پس فایل خودش را هم می‌شمارد. هرس درست کار
می‌کند؛ فقط عدد گمراه‌کننده است.

### `pg_dump` توابع `app_*` را با پیشوند `public.` می‌نویسد

`grep -c "CREATE FUNCTION app_"` صفر می‌دهد و به‌نظر می‌رسد بکاپ ناقص است.
الگوی درست `CREATE FUNCTION` تنهاست، یا `public.app_`.

### 🔴 فایل compose با `:?required` قبل از `.env.prod`، بکاپ را می‌خواباند

`docker compose` کل فایل را interpolate می‌کند، حتی وقتی فقط می‌خواهی
`exec` روی یک سرویس بزنی. پس یک `${VAR:?required}` که مقدارش هنوز در
`.env.prod` نیست، **هر دستور compose ای را رد می‌کند** — نه فقط `up`.

در استقرار فاز ۸ این شکل را داشت: `./backup-database.sh` با
`required variable SMS_TEMPLATE_BEFORE_EXPIRY is missing` رد شد، خطایی که
هیچ ربطی به بکاپ ندارد و به فایلی اشاره می‌کند که آن اسکریپت نمی‌شناسد.

⚠️ **اثر پنهانش بدتر است:** بکاپ شبانه‌ی `0 3` و `subscription-cron.sh`
هم از همان لحظه می‌شکستند — بی‌صدا، چون خروجی cron به لاگ می‌رود و کسی
شب‌ها نگاهش نمی‌کند.

**ترتیب درست:** `.env.prod` اول، فایل compose بعد.

---

## ۱۰. باگ‌هایی که در مسیر پیدا و رفع شدند

| باگ                                             | محل                                   | اثر                                                 |
| ----------------------------------------------- | ------------------------------------- | --------------------------------------------------- |
| نشت موجودی در ویرایش فاکتور فروش                | `saleInvoiceController.update`        | هر ویرایش ناموفق موجودی را دائمی بالا می‌برد        |
| `reference_id` همیشه `null`                     | هر سه کنترلر فاکتور                   | تاریخچه‌ی کالا شماره فاکتور نشان نمی‌داد            |
| خدمات به کالای بی‌ربط وصل                       | `repairInvoiceController.getById`     | JOIN بی‌قید روی `item_id` پلی‌مورفیک                |
| جابه‌جایی ستون در نگاشت                         | `customerController.getDevices`       | `created_at` به‌عنوان `image_path`                  |
| فاکتور بدون قلم غیرقابل حذف                     | خرید و فروش                           | اقلام را می‌خواند نه فاکتور را                      |
| فیلتر `role` نادیده گرفته می‌شد                 | `personnelController.getAll`          | فهرست تکنسین‌ها مدیران را هم نشان می‌داد            |
| رمز در لاگ                                      | `authController`                      | ۲۰ کاراکتر اول هش bcrypt چاپ می‌شد                  |
| ساخت جدول در زمان اجرا                          | `serviceController.getAll`            | هر درخواست `CREATE TABLE IF NOT EXISTS`             |
| `quickSale` به قیمت خرید                        | `itemController`                      | گزارش سود حاشیه‌ی صفر نشان می‌داد                   |
| سوپرادمین می‌توانست خودش را تنزل دهد            | `personnelController.update`          | راه برگشتی نبود                                     |
| **۹ کوئری از ۱۷ بدون scope**                    | `reportController.getDashboardStats`  | داشبورد یک کارگاه ارقام همه را نشان می‌داد          |
| **حذف مشتری ۲۰۰ برای شناسه‌ی ناموجود**          | `customerController.remove`           | همان عملیات در devices و items ۴۰۴ می‌داد           |
| **logout همه‌ی دستگاه‌ها را بیرون می‌انداخت**   | `authController.logout`               | ابطال به‌جای حذف، تب بازمانده = سیگنال سرقت         |
| **username غیرموبایل در ساخت پرسنل**            | `schemas/personnel.ts`                | حسابی که ساخته می‌شد و هیچ‌کس نمی‌توانست واردش شود  |
| **multer context را قطع می‌کرد**                | مسیرهای آپلود                         | هر کوئری پشت آپلود خطا می‌داد                       |
| **`refresh` دو بار پاسخ می‌فرستاد**             | `authController.refresh`              | دو session به‌ازای هر رفرش صفحه، نامرئی برای کاربر  |
| **کلاینت رهاشده ۵۰۰ لاگ می‌کرد**                | `issueSession`                        | خطا برای درخواستی که هرگز شکست نخورده بود           |
| **عکس‌ها بدون `.rotate()` کج ذخیره می‌شدند**    | `imageController`                     | ۱۱ از ۱۵ عکس واقعی چرخیده نمایش داده می‌شد          |
| **`JWT_SECRET` به رشته‌ی مخزن fallback می‌کرد** | `middleware/auth`                     | متغیر خالی = هر کسی توکن هر کارگاهی را می‌سازد      |
| **`trust proxy` بولی بود**                      | `app.ts`                              | سقف per-IP در `send-otp` با یک هدر جعلی دور می‌خورد |
| **`migrate` تگ ایمیج نداشت**                    | `docker-compose.prod.yml`             | روی سرور دنبال `./backend` می‌گشت که وجود ندارد     |
| **آزمایشی دو بار داده می‌شد**                   | `app_create_workspace` + `startTrial` | هر کارگاه تازه ۶۱ روز می‌گرفت به‌جای ۳۰             |
| **سقف `maxUses` کد تخفیف بی‌اثر بود**           | `usableDiscountCode`                  | کد پخش‌شده در تلگرام تخفیف دائمی می‌شد              |
| **`createdBy` روی `payments` اجباری بود**       | `schema.prisma`                       | حذف کارمندی که یک بار خرید کرده، خطای FK می‌داد     |
| **`discount_code_uses` هرگز نوشته نمی‌شد**      | `subscriptionController`              | هر کد تخفیف بی‌نهایت بار قابل استفاده بود           |
| **`ZIBAL_MERCHANT=ZIBAL`**                      | `.env` · `.env.example`               | هر پرداخت با کد ۱۰۴ رد می‌شد                        |
| **کد تخفیف پیش از پرداخت نمایش داده نمی‌شد**    | `Subscription.tsx`                    | تنها راه فهمیدن اعمال تخفیف، رفتن به درگاه بود      |

---

## ۱۱. وضعیت تست‌ها

### تست‌های واحد (`pnpm test`)

```
~۴۲ فایل · ~۵۵۷ تست · همه پاس · Prisma، storage، sms، zibal و imageProfile با mock
```

⚠️ اعداد تقریبی‌اند — بعد از هر تسک با `pnpm test` تأییدشان کن.

### تست‌های یکپارچگی (`pnpm test:integration`)

```
~۱۰ فایل · ~۱۲۸ تست (۳ skip) · دیتابیس واقعی dofixo_test
```

| فایل                            | پوشش                                             |
| ------------------------------- | ------------------------------------------------ |
| `smoke.test.ts`                 | اتصال با `dofixo_app`، شمارش policy، دو کارگاه   |
| `isolation.test.ts`             | جدول‌محور: ۹ منبع × (فهرست، خواندن، ویرایش، حذف) |
| `isolationSpecialCases.test.ts` | `settings`، ۱۷ کوئری داشبورد، `personnel`        |
| `invoiceNumbering.test.ts`      | شمارنده، استقلال کارگاه‌ها، ۱۰ درخواست همزمان    |
| `register.test.ts`              | ثبت‌نام، کارگاه یتیم، ایزوله بودن کارگاه تازه    |
| `refreshToken.test.ts`          | کوکی، چرخش، تشخیص سرقت، logout تک‌session        |
| `resetPassword.test.ts`         | مصرف کد، حذف همه‌ی session ها، عدم صدور session  |
| `subscription.test.ts`          | ۴۰۲، مهلت ارفاق، مسیرهای باز، `neverExpires`     |
| `subscriptionPayment.test.ts`   | checkout تا verify، کد تخفیف، نقش، کارگاه منقضی  |
| `referral.test.ts`              | ثبت رابطه، تخفیف ۱۰٪، پاداش روی کارگاه دیگر      |

⚠️ تست‌های یکپارچگی **به Arvan و زیبال واقعی نمی‌خورند** — `lib/storage` و
`lib/zibal` mock اند.

⚠️ **`helpers.ts` به هر کارگاه fixture یک آزمایشی زنده می‌دهد.** بدون آن هر
نوشتنی در هر سوئیت ۴۰۲ می‌گیرد — که نگهبان درست کار کردن است و fixture غلط
بودن. `truncateAll()` همه‌ی جدول‌ها را خالی می‌کند، پس `plans` هم مثل
`roles` باید در تست دوباره ساخته شود.

**سه باگ واقعی را همین سوئیت پیدا کرد** و هیچ‌کدام با mock دیدنی نبودند:
آزمایشی دوبار (نصفش در SQL بود)، سقف کد تخفیف (mock هر عددی بگویی
برمی‌گرداند)، و مهاجرتی که در پوشه‌ی اشتباه ساخته شده بود.

⚠️ **و یک باگ را هیچ تستی پیدا نکرد** — اینکه `discount_code_uses` هرگز
نوشته نمی‌شد. جدول policy و `REVOKE` و تست یکپارچگی داشت، ولی آن تست ردیف را
دستی می‌ساخت تا سقف را بسنجد. **خرید دستی در مرورگر پیدایش کرد.** درسش این
است: تستی که وضعیت را خودش می‌سازد، نمی‌پرسد آیا کد واقعی آن وضعیت را
می‌سازد یا نه.

**اضافه کردن منبع REST جدید:** یک خط در آرایه‌ی `resources` در `isolation.test.ts`.

**آنچه تست نمی‌شود:** فرانت، اسکریپت‌های `ops/`، اتمی بودن تراکنش‌ها در حالت شکست،
`PUT /:id` کامل سه نوع فاکتور، و اتصال واقعی به object storage.

### تأییدهای دستی که انجام شده‌اند

- **بازیابی کامل دیتابیس:** داده + policyها + توابع `SECURITY DEFINER` برگشتند
- **بازیابی یک کارگاه:** کاربر با رمز قبلی وارد شد، عکس‌ها با کلید قدیمی لود شدند،
  و کارگاه دیگری داده‌اش را ندید
- **بازیابی دسترسی:** رمز عوض شد، session قبلی بسته شد، حساب غیرفعال رد شد
- **بازیابی بکاپ پروداکشن:** dump روی لپ‌تاپ رمزگشایی شد — ۲۱ policy، چهار
  تابع `app_*`، سه ردیف `roles` با متن فارسی سالم
- **۷.۰ روی پروداکشن:** عکس عمودی درست نمایش داده شد، thumbnail لود شد، حذف
  عکس هر دو شیء را برد، ردیف‌های قدیمی بدون thumbnail به عکس کامل fallback کردند

---

## ۱۲. توصیه برای شروع نشست بعدی

۱. سه فایل `CLAUDE.md`، `Roadmap.md` و `RULES.md` را بفرست
۲. این سند را بفرست
۳. تسک را انتخاب کن

### مسیر پیشنهادی

**۱. 🔴 استقرار** — فاز ۸ کامل است و روی سرور نیست
**۲. AUTH.1 تا AUTH.5** — بدهی ۳۱، قبل از اینکه کارگاه واقعی پرسنل بسازد
**۳. یک پرداخت واقعی روی پروداکشن** — قبل از تبلیغ
**۴. فاز ۱۰** — شانزده باگ فرانت، هرکدام مستقل و کوچک
**۵. ۷.۷** — GitHub Actions با `pnpm test:all`
**۶. فاز ۶** — Vitest برای فرانت — فاز ۸ هیچ تست فرانتی ندارد

### ⚠️ کارهای زمان‌دار که فراموش می‌شوند

| کِی                    | چه                                      |
| ---------------------- | --------------------------------------- |
| 🔴 **۲۹ سپتامبر ۲۰۲۶** | انقضای کارگاه پروداکشن — پایین را بخوان |
| **۲۵ اکتبر ۲۰۲۶**      | تمدید گواهی TLS (انقضا ۲۵ نوامبر)       |
| هر روز، هفته‌ی اول     | `cat ~/dofixo-backup.log` روی سرور      |
| قبل از رشد واقعی       | سهمیه‌ی فضا (بدهی ۲۹ و ۳۵)              |

### ✅ انقضای ۲۹ سپتامبر رفع شد

مهاجرت ۸.۱ در backfill خودش روی هر کارگاه موجود `never_expires = TRUE`
گذاشت. در استقرار ۲ سپتامبر تأیید شد: هر سه کارگاه `t` گرفتند.

⚠️ `expires_at` قدیمی هنوز روی ردیف‌ها هست ولی `never_expires` بی‌اثرش
می‌کند. این عمدی است — تاریخ پاک نمی‌شود تا اگر روزی معافیت برداشته شد،
معلوم باشد از کجا آمده.

### وضعیت استقرار فاز ۸ — ۲ سپتامبر ۲۰۲۶

- ✅ شش متغیر تازه در `.env.prod` (نه هفت — `APP_URL` مشتق می‌شود)
- ✅ `docker-compose.prod.yml` با `environment:` کامل و `:?required`
- ✅ چهار مهاجرت اجرا شدند
- ✅ `never_expires = t` روی هر سه کارگاه · سه پلن · **۲۹ policy**
- ✅ `rls-check.sql` → `(0 rows)`
- ✅ `BACKEND_SERVICE=backend` در `backup.env`
- ✅ اجرای دستی cron: `notified=0 status=0 deleted=0 settled=0 failures=0`
- ✅ cron نصب شد روی `0 4` (بکاپ `0 3`)
- ✅ یک پرداخت واقعی — `verified`، با `refNumber` و `cardNumber`
- ✅ رفرال end-to-end روی پروداکشن: ۳۰ روز روی کارگاه دعوت‌کننده
- 🔴 **پیامک تأیید پرداخت نرفت** — بدهی ۴۰، تسک ۸.۱۰

### کارگاه‌های روی پروداکشن

| شناسه | نام       | یادداشت                                          |
| ----- | --------- | ------------------------------------------------ |
| ۱     | نینی جوجو | کارگاه تست اصلی · کد رفرال `XWJCHY`              |
| ۲     | مهدی      | `H56V3H`                                         |
| ۳     | —         | با کد رفرال ثبت‌نام کرد · پرداخت واقعی ۲ سپتامبر |

⚠️ نسخه‌ی قبلی این سند فقط از یک کارگاه حرف می‌زد.

### مسیر پیشنهادی برای نشست بعد

**۱. ۸.۱۰** — پیامک تأیید پرداخت (بدهی ۴۰). مشتری واقعی الان تأییدیه
نمی‌گیرد.
**۲. AUTH.1 تا AUTH.5** — بدهی ۳۱، قبل از اینکه کارگاه واقعی پرسنل بسازد
**۳. بدهی ۴۱** — timeout زیبال
**۴. فاز ۱۰** — شانزده باگ فرانت
**۵. ۷.۷** — GitHub Actions با `pnpm test:all`
**۶. فاز ۶** — Vitest برای فرانت

### ⚠️ کارهای زمان‌دار

| کِی               | چه                                                         |
| ----------------- | ---------------------------------------------------------- |
| **۲۵ اکتبر ۲۰۲۶** | تمدید گواهی TLS (انقضا ۲۵ نوامبر)                          |
| **فردا صبح**      | `tail ~/dofixo-subscription.log` — اولین اجرای خودکار cron |
| هفتگی             | `tail ~/dofixo-backup.log`                                 |
| قبل از رشد واقعی  | سهمیه‌ی فضا (بدهی ۲۹ و ۳۵)                                 |

### دستور روزمره روی سرور

```bash
cd ~/dofixo
alias dc='docker compose -f docker-compose.prod.yml --env-file .env.prod'
```

⚠️ `alias` هر بار از بین می‌رود و فقط داخل `~/dofixo` کار می‌کند —
`--env-file .env.prod` مسیر نسبی است.
