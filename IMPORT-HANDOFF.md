# Dofixo — سند انتقال ایمپورت (تسک ۵.۸)

> این سند برای ادامه‌ی کار ایمپورت داده‌ی «گروه مهندسی زیمنس پارت» از نسخه‌ی
> تک‌مستأجر Dofixo (SQLite) به SaaS نوشته شده است.
>
> **تاریخ:** ۱۶ سپتامبر ۲۰۲۶ · **برنچ:** `claude/bold-gauss-get1gp`
>
> در نشست بعد این سند را به‌علاوه‌ی `CLAUDE.md`، `RULES.md`، `Roadmap.md` و
> `HANDOFF.md` بفرست.

---

## ۱. هدف

انتقال داده‌ی واقعی یک تعمیرگاه از نرم‌افزار تک‌مستأجر قدیمی به یک کارگاه
در SaaS. داده روی لپ‌تاپ Reza است (لینوکس)، نه روی سرور.

**حجم:** ۲۹۹ مشتری · ۲۰۵۴ دستگاه · ۳۶۷۸ عکس (۱۱ گیگ) · ۷۰ واگذاری · ۶ کاربر

---

## ۲. وضعیت فعلی — کجا هستیم

| مرحله | وضعیت |
| --- | --- |
| تسک ۲.۹ (شماره پذیرش) | ✅ کامل و **کامیت‌شده** |
| اسکریپت `import-legacy.ts` | ✅ نوشته شده · 🔴 **کامیت نشده** |
| تست کامل روی لوکال | ✅ همه‌ی ردیف‌ها + ۵۰ عکس |
| `media` کامل روی لپ‌تاپ | ✅ ۳۶۵۸ از ۳۶۷۸ (۲۰ فایل خراب) |
| مستندات (`Roadmap`/`HANDOFF`) | 🔴 **نوشته نشده** |
| راهنمای `ops/` | 🔴 **نوشته نشده** |
| اجرا روی پروداکشن | 🔴 **انجام نشده** |

🔴 **کار بعدی:** مستندات → کامیت → آماده‌سازی پروداکشن → اجرا

---

## ۳. تسک ۲.۹ — پیش‌نیازی که انجام شد

**چرا لازم بود:** `devices.id` هم کلید جانشین بود و هم شماره‌ی پذیرشی که روی
برگه نوشته و به مشتری گفته می‌شود. کلید از sequence مشترک کل پلتفرم می‌آید،
پس شماره‌ی هر کارگاه به فعالیت بقیه وابسته بود.

**راه‌حل:** `Device.receptionNumber` + `Workspace.deviceSeq`، هم‌شکل ۲.۸.

- مهاجرت `20260916082931_device_reception_number` (سه‌مرحله‌ای: nullable →
  backfill با `ROW_NUMBER() OVER (PARTITION BY workspace_id ORDER BY id)` →
  `NOT NULL` + یکتایی `(workspaceId, receptionNumber)`)
- `utils/deviceNumber.ts` — `nextReceptionNumber(tx, workspaceId)`
- `deviceController.create` حالا داخل `runInWorkspaceTransaction`
- جست‌وجوی عددی از `id` به `receptionNumber` عوض شد
- هفت نقطه‌ی نمایش در فرانت + دو ستون Excel + پارامتر `#NUMBER#` پیامک
- تست‌ها: `deviceNumber.test.ts` (۳) و `integration/deviceNumbering.test.ts` (۵)

⚠️ **سود مستقیمش برای ایمپورت:** دستگاه‌های قدیم شماره‌ی ۱ تا ۲۰۵۴ می‌گیرند
بدون اینکه هیچ شناسه‌ای دستکاری شود.

---

## ۴. اسکریپت ایمپورت

**مسیر:** `backend/scripts/import-legacy.ts`
**وابستگی تازه:** `better-sqlite3` + `@types/better-sqlite3` (devDependency)

⚠️ بعد از نصب `pnpm approve-builds` لازم بود — ماژول native است.

### سه فرمان

```bash
# فقط می‌خواند و گزارش می‌دهد. هیچ نوشتنی.
pnpm exec tsx scripts/import-legacy.ts plan \
  --db <sqlite> --uploads <dir> --users <json>

# پردازش عکس‌ها به staging. CPU خالص، قابل ازسرگیری.
pnpm exec tsx scripts/import-legacy.ts media \
  --db <sqlite> --uploads <dir> --out <staging> [--limit N] [--concurrency 3]

# نوشتن ردیف‌ها + آپلود عکس‌ها
pnpm exec tsx scripts/import-legacy.ts apply \
  --db <sqlite> --staging <dir> --users <json> --workspace-id N \
  [--dry-run] [--images-only]
```

### تصمیمات طراحی

**سه فرمان جدا، نه یکی.** `media` نیم ساعت CPU است و `apply` با شبکه و
دیتابیس کار می‌کند؛ یک اجرای یکپارچه که دوسوم راه بشکند، راه ادامه ندارد.

**`media` قابل ازسرگیری است.** نام خروجی از `id` عکس مبدأ ساخته می‌شود
(`3678.webp` و `3678-thumb.webp`)، پس فایلی که هست رد می‌شود. uuid واقعی در
`apply` ساخته می‌شود، جایی که کلید شیء هم ساخته می‌شود.

**`apply` دو فاز دارد:**
- فاز ۱ — همه‌ی ردیف‌ها در **یک تراکنش** با `timeout: 600_000`
- فاز ۲ — آپلود عکس‌ها **بیرون** تراکنش، ۶ تا موازی

⚠️ دلیل جدایی: ۳۶۷۸ آپلود داخل تراکنش یعنی ساعت‌ها قفل — همان اشتباهی که
۸.۱۰ گرفت و ۱۲.۷ مکتوبش کرد.

**`--images-only` برای ادامه بعد از قطعی.** نگاشت دستگاه‌ها از
`receptionNumber` بازسازی می‌شود، چون عمداً همان `id` مبدأ است.

**شماره‌ی پذیرش از `id` مبدأ می‌آید، نه از شمارنده.** این‌ها رکورد تاریخی‌اند
و شماره‌شان روی برگه‌ی دست مشتری نوشته شده. در پایان `deviceSeq` روی ۲۰۵۴
ست می‌شود تا دستگاه بعدی ۲۰۵۵ بگیرد.

**بررسی‌های قبل از شروع:**
1. کارگاه مقصد وجود دارد
2. کارگاه مقصد **خالی** است (صفر مشتری و دستگاه) — مگر `--images-only`
3. هیچ‌کدام از نام‌های کاربری در **کل پلتفرم** گرفته نشده باشد

⚠️ بررسی سوم با **اتصال مالک** (`DATABASE_URL`) انجام می‌شود، چون یکتایی
نام کاربری سراسری است و RLS آن را به کارگاه مقصد محدود می‌کند. تابع
`takenUsernames`.

### چه چیزی منتقل می‌شود

| منتقل می‌شود | نمی‌شود |
| --- | --- |
| کاربران (۶ نفر) | فاکتورها (داده‌ی آزمایشی) |
| مشتریان (۲۹۹) | انبار و دسته‌بندی (داده‌ی آزمایشی) |
| دستگاه‌ها (۲۰۵۴) | خدمات (`populateWorkspace` همان چهارتا را می‌سازد) |
| عکس‌ها (۳۶۵۸) | لوگو (بعداً دستی) |
| واگذاری‌ها (۷۰) | `backups` |
| تنظیمات (نام، تلفن، آدرس، وب‌سایت) | |

---

## ۵. فایل کاربران

**مسیر:** `~/dofixo-import/users.json` — **بیرون از مخزن، `chmod 600`**

```json
[
  { "oldId": 9,  "fullName": "رضا عبدالهی",     "username": "09219811980", "password": "", "role": "owner" },
  { "oldId": 11, "fullName": "مهندس جعفری",     "username": "09121820830", "password": "Jafari1234",   "role": "admin" },
  { "oldId": 5,  "fullName": "شاقلانی",          "username": "09923348078", "password": "Nastaran1234", "role": "technician" },
  { "oldId": 6,  "fullName": "یگانه جعفری",      "username": "09335374224", "password": "Yeganeh1234",  "role": "technician" },
  { "oldId": 7,  "fullName": "ریحانه حیدری",     "username": "09399639268", "password": "Reyhaneh1234", "role": "technician" },
  { "oldId": 8,  "fullName": "علیرضا اسماعیلی", "username": "09121822435", "password": "Alreza1234",   "role": "technician" },
  { "oldId": 43, "fullName": "آرزو جعفری",       "username": "09199889539", "password": "Arezu1234",    "role": "technician" }
]
```

⚠️ **`role: "owner"` ساخته نمی‌شود** — Reza خودش از رابط ثبت‌نام می‌کند تا
`populateWorkspace` اجرا شود (تنظیمات، چهار خدمت، آزمایشی، کد رفرال، کیف پول).
خط `oldId: 9` فقط برای نگاشت واگذاری‌ها در فایل است.

⚠️ **یگانه شماره‌ی تازه گرفت** (`09335374224` به‌جای `09330074224`) چون
شماره‌ی قبلی‌اش روی پروداکشن در کارگاه ۱ («نینی جوجو») حساب دارد.

⚠️ **صدرا حسین‌پور (۱۹) و پارسا جعفری (۲۰) منتقل نمی‌شوند** — دیگر آنجا کار
نمی‌کنند و هیچ واگذاری هم ندارند.

🔴 **رمزها قابل حدس‌اند** («نام + ۱۲۳۴»). برای رمز موقتی که همان روز اول عوض
شود قابل قبول است، به شرطی که واقعاً عوض شود. بعد از ایمپورت به هر کدام
گفته شود رمزش را از «تغییر رمز» عوض کند.

---

## ۶. مسیرهای مبدأ (روی لپ‌تاپ Reza)

```
دیتابیس:  ~/Downloads/Feem/repair-system/backend/src/repair_system.db
عکس‌ها:    ~/Downloads/Feem/repair-system/backend/src/uploads/devices/
staging:  ~/dofixo-import/staging/        (۱٫۲ گیگ · ۷۳۱۶ فایل)
کاربران:  ~/dofixo-import/users.json
```

⚠️ مخزن قدیم روی برنچ `custom/siemens-part` با ۹۱ فایل تغییریافته است.
**به آن دست نزن** — نه commit، نه checkout. فقط خواندنی است.

---

## ۷. نتیجه‌ی تست روی لوکال ✅

کارگاه مقصد: **کارگاه ۲** در `dofixo_dev` (نام «خودم»، مالک `09219811980`)

| بررسی | نتیجه |
| --- | --- |
| ردیف‌ها | ۲۹۹ مشتری · ۲۰۵۴ دستگاه · ۷۰ واگذاری — در **۱ ثانیه** |
| شماره‌ی پذیرش | ۱ تا ۲۰۵۴، هر ۲۰۵۴ یکتا |
| `device_seq` | ۲۰۵۴ ✅ |
| وضعیت‌ها | عیناً همان توزیع مبدأ |
| تاریخ خروج | ۵۸ دستگاه · صفر بدون تاریخ ورود |
| عکس‌ها | ۵۰ تا آپلود و در مرورگر دیده شد |
| ورود با حساب تازه | ✅ `09121820830` / `Jafari1234` — نقش ادمین درست |
| تنظیمات | «گروه مهندسی زیمنس پارت» ✅ |

---

## ۸. 🔴 بیست عکس خراب — قابل بازیابی نیستند

`media` کامل ۳۶۵۸ از ۳۶۷۸ را پردازش کرد. بیست فایل JPEG معیوب‌اند و **در
نمایشگر تصویر هم باز نمی‌شوند** — یعنی فایل‌ها واقعاً از بین رفته‌اند، نه
مشکل اسکریپت.

```
315-2.jpg   Corrupt JPEG data: premature end of data segment
907-1.jpg   Invalid SOS parameters for sequential JPEG
908-1.jpg · 912-1.jpg · 913-1.jpg · 915-1.jpg · 916-1.jpg
918-1.jpg · 921-1.jpg · 922-1.jpg · 925-1.jpg · 925-2.jpg
926-1.jpg · 927-1.jpg · 929-1.jpg · 930-1.jpg · 933-1.jpg
933-2.jpg · 985-1.jpg · 985-2.jpg
```

⚠️ هجده‌تا پشت سر هم‌اند (۹۰۷ تا ۹۳۳) — احتمالاً یک انتقال ناقص در گذشته.

**تصمیم:** رها می‌شوند. دستگاه‌های مربوطه قدیمی‌اند و بدون عکس منتقل می‌شوند.
`apply` ردیفشان را هم نمی‌سازد و در گزارش پایانی به‌عنوان «جا افتاد» شمرده
می‌شوند.

---

## ۹. اعداد کارایی

| کار | سرعت روی لپ‌تاپ |
| --- | --- |
| `media` کامل | **۲۱ دقیقه** (۳۶۰۸ عکس) · خروجی ۱٫۲ گیگ از ۱۱ گیگ |
| نوشتن ردیف‌ها | **۱ ثانیه** (~۶۱۰۰ درج در یک تراکنش) |
| آپلود عکس | **~۳ ثانیه هر کدام** با ۶ موازی → ~۳ ساعت برای همه |

🔴 **آپلود روی لپ‌تاپ کند است و دو بار از ۶۰ ثانیه timeout رد شد.** گلوگاه
پهنای باند خروجی خانگی است، نه تأخیر. **روی سرور بسیار سریع‌تر خواهد بود**
چون آروان داخلی است.

---

## ۱۰. 🔴 آماده‌سازی پروداکشن — قبل از هر چیز

### کارگاه‌های موجود

| شناسه | نام | تصمیم |
| --- | --- | --- |
| ۱ | نینی جوجو | **می‌ماند** (کارگاه تست) |
| ۲ | مهدی | **می‌ماند** (دوست Reza) |
| ۳ | تعمیرگاه رضا | 🔴 آزاد شود — شماره‌ی `09219811980` لازم است |
| ۴ | رضا ایرانسل | آزاد شود (`09028493475`) |

**روش پیشنهادی: سنگ‌قبر، نه حذف کامل.**

```sql
-- نام کاربری آزاد می‌شود، دفتر مالی دست‌نخورده می‌ماند
UPDATE users SET username = username || '-old', is_active = false
WHERE workspace_id IN (3, 4);

UPDATE workspaces SET status = 'deleted', deleted_at = now()
WHERE id IN (3, 4);
```

⚠️ **چرا حذف کامل نه:** نقش اپ روی `payments` مجوز `DELETE` ندارد — عمدی،
چون دفتری که پاک شود دفتر نیست. کارگاه ۳ یک پرداخت واقعی ۲ سپتامبر دارد.
سنگ‌قبر همان کاری است که ۸.۷ برای هر کارگاه واقعی می‌کند.

⚠️ اگر داده‌شان هم باید برود، `utils/workspaceDeletion.ts` را استفاده کن —
نه `DELETE` دستی. بیست جدول با `Restrict` به هم وصل‌اند و عکس‌های آروان باید
**قبل** از ردیف‌ها بروند وگرنه کلیدها برای همیشه گم می‌شوند.

### ترتیب اجرا روی پروداکشن

```
۱. سنگ‌قبر کارگاه ۳ و ۴ (بالا)
۲. بکاپ دستی:  cd ~/dofixo && ./backup-database.sh
۳. ثبت‌نام از رابط با 09219811980 → کارگاه تازه، Reza سوپرادمین
۴. شناسه‌ی کارگاه تازه را بگیر
۵. انتقال staging به سرور (rsync -P ~/dofixo-import/staging/)
۶. انتقال repair_system.db و users.json
۷. plan  → با چشم بخوان
۸. apply --dry-run → با چشم بخوان
۹. apply
۱۰. تأیید چشمی در مرورگر
```

🔴 **اسکریپت روی پروداکشن باید داخل کانتینر اجرا شود** — پورت ۵۴۳۲ از بیرون
بسته است و فقط کانتینر به آروان می‌رسد.

⚠️ **ولی `DATABASE_URL` در کانتینر backend نیست** (عمدی — فقط در سرویس
`migrate`). `takenUsernames` به آن نیاز دارد. دو راه: اجرا از سرویس
`migrate`، یا دادن متغیر به‌صورت دستی در همان دستور.

⚠️ فایل SQLite و پوشه‌ی staging باید mount شوند.

⚠️ **`better-sqlite3` در ایمیج پروداکشن نیست** — devDependency است و
Dockerfile چندمرحله‌ای آن را نمی‌برد. یا ایمیج تازه با آن ساخته شود، یا
اسکریپت از سرویس `migrate` اجرا شود که `node_modules` کامل دارد.

---

## ۱۱. کارهای باقی‌مانده

```markdown
- [ ] مستندات: Roadmap.md تسک ۵.۸
- [ ] مستندات: HANDOFF.md بخش ایمپورت
- [ ] راهنمای ops/import-legacy.md برای اجرای پروداکشن
- [ ] کامیت اسکریپت (هنوز کامیت نشده)
- [ ] تعیین تکلیف better-sqlite3 در ایمیج پروداکشن
- [ ] سنگ‌قبر کارگاه ۳ و ۴
- [ ] اجرا روی پروداکشن
- [ ] لوگوی شرکت (دستی، بعد از ایمپورت)
```

⚠️ **یک نقص کوچک ثبت‌شده:** گزارش اسکریپت «۷ کاربر» می‌گوید در حالی که
دیتابیس ۸ نشان می‌دهد — عدد گزارش اندازه‌ی **نگاشت** است نه تعداد کاربران
کارگاه. گمراه‌کننده است ولی بی‌خطر.

⚠️ **`config()` قبل از `import`ها نشسته** در اسکریپت. کار می‌کند (esbuild
ایمپورت‌ها را بالا می‌برد) ولی خواندنش عجیب است. تمیزکاری اختیاری.

---

## ۱۲. دام‌هایی که در این نشست خوردیم

🔴 **کاراکتر `<` در کپی از چت گم می‌شود** — این بار سه بار اتفاق افتاد
(`Omit<`، `Pick<`). الگو: `<` که در **انتهای خط** باشد می‌افتد؛ وسط خط
سالم می‌ماند. بعد از هر کپی:
```bash
grep -n "Omit<\|Pick<\|Map<\|Promise<" <file>
```

⚠️ **`prisma generate` با `EACCES`** — فایل‌های `src/generated/prisma` مالکشان
root شده بود. درمان: `sudo chown -R "$USER":"$USER" backend`

⚠️ **`pgcrypto` روی Postgres توسعه نصب نیست** — برای ساخت دستی کد OTP باید
از `hashOtpCode` خود پروژه استفاده کرد، نه `digest()` در SQL.

⚠️ **بررسی امنیتی که مسیر دوم را می‌بندد** — بررسی یکتایی نام کاربری در
`--images-only` باید رد شود، چون کاربران را خود اسکریپت ساخته.

⚠️ **گرپ روی نام متغیر کافی نیست.** در تسک ۲.۹ دو نقطه از گرپ `device.id`
در رفتند و فقط با گرپ روی **عنوان فارسی** («پذیرش») و نگاه کردن در مرورگر
پیدا شدند.

---

## ۱۳. دستور سریع برای شروع نشست بعد

```bash
cd /Programming/Code/Dofixo
git status --short
git log --oneline -3

# وضعیت لوکال
docker compose exec -T postgres psql -U dofixo -d dofixo_dev -c "
SELECT w.id, w.name,
  (SELECT count(*) FROM customers c WHERE c.workspace_id=w.id) AS customers,
  (SELECT count(*) FROM devices d WHERE d.workspace_id=w.id) AS devices,
  (SELECT count(*) FROM device_images i WHERE i.workspace_id=w.id) AS images,
  w.device_seq
FROM workspaces w ORDER BY w.id;"

# staging سر جایش هست؟
du -sh ~/dofixo-import/staging && ls ~/dofixo-import/staging | wc -l
```
