node_modules
dist
backups
src/uploads
.env
*.db

# Reinstalled inside the image for linux/glibc. Copying the host's would
# bring sharp's Arch binaries into a Debian container.
node_modules
dist

# Regenerated by `prisma generate` in the build stage. Copying the host's
# risks shipping a client that no longer matches schema.prisma.
src/generated

# Never, under any circumstance.
.env
.env.*
*.pem
*.key

backups
src/uploads
src/backups

src/__tests__
jest.config.js
jest.integration.config.js
jest.integration.globalSetup.ts
jest.integration.setup.ts
jest.setup.ts

.git
.gitignore
*.log
Dockerfile
Dockerfile.dev
.dockerignore